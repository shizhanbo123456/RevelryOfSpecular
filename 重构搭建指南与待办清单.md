# 搭建与待办清单（当前状态）

> 本清单 = 需要你在 Unity 编辑器里完成的事（代码无法自动化的部分）+ 代码侧仍未接线的模块 + 写后续代码要遵守的约定。
> 口径：**只写当前状态与待办，不写变更历史**（"曾经如何/何时修好"看 git log）。上次核对：2026-09-24。

---

## 一、待你在 Unity 里完成

### 1. 生成网络代码 ——【暂缓，最后一步】
- 打开项目 `RevelryOfSepulcher`（Unity 2022.3.62）→ 菜单 **`Ens/GenerateCode`**（另有 `Ens/CleanGeneratedCode`）→ 生成物落在 `Assets/EnsNetcode/Gen/NetworkManager.Generated.cs`
- 16 个 `[Rpc]`（6 上行 + 10 下行）依赖它
- ✅ **生成器路径无需修改**：`Editor/CodeGenerator.cs:12-13` 的 `sourceDir = "Assets/Scripts"`、`genDir = "Assets/EnsNetcode/Gen"` 与实际目录一致（库侧 `5c72f63` 已修正过）
- 现状：该文件当前是 3 行注释桩（内容已清空），两仓工作区均干净
- 📍 库位置：`Assets/EnsNetcode`（与 `Assets/Scripts` 平级，独立 git 仓 `ENS-Netcode`、分支 master，保留自己的远程）

### 2. 客户端场景：删除 ClientDisplayManager 组件 ——【待做】
- [ ] `MainScene.unity` 上挂着 `ClientDisplayManager` 的对象，**把该组件删掉**（该类已是普通 C# 类，不再挂场景，会显示 Missing (Mono Script)）
- 不用动：`ClientLogicManager` 仍是 MonoBehaviour 且已挂场景；其余 5 个子管理器由它运行时创建，不会出现在 Inspector 里

### 3. 动画状态机补挂 AnimEvent ——【待做，优先级高】
`Assets/Files/Anim/CharacterAnim.controller` 共 **33 个状态**：14 个攻击状态已挂 `AnimAttackEvent`，**其余 19 个状态一个行为脚本都没有**。

代码是**纯推送制**（`EntityAnim.CurrentState` 与 `currentAnimId` 只由 `AnimEvent.OnStateEnter` 更新，`Do*` 方法只写状态机参数），漏挂的后果有两层：
1. **该状态的动画不会同步给客户端** —— 客户端按 hash 播放，会停在最后一次攻击动画上（走/跑/跳/受击/死亡都不对）
2. **霸体等级判错** —— `EntityData.GetEndureLevel()` 读 `anim.CurrentState`，状态不更新就会按旧状态判

- [ ] 给需要同步的状态各自挂 `AnimEvent`，Inspector 里把 `state` 枚举设对（Spawn / Motion / Attack / Hit / Die）
- [ ] 14 个攻击状态确认 `AnimAttackEvent` 参数：`type`（AttackType）与 `threshold`（伤害判定的归一化进度），双段攻击再设 `useHit2` / `threshold2`
- [ ] 移动状态的**速度声明参数**（行为脚本侧已接好，只差"挂上去 + 配参数"）：
  - `AnimRunEvent.speedFactor`：逐状态倍率（走/跑靠它区分）
  - `AnimJumpEndEvent.moveAfterLand`：**默认 false = 落地即停**；要落地后继续走就勾上
  - `AnimRollEvent.speedCurve`：**不配 = 空曲线 = 翻滚零位移**
- [ ] **所有动画机地位相同、不分主次**：实体上每个 Animator 各自挂行为脚本，需要同步状态 / 声明速度的状态一个都别漏（详见《代码架构说明》`[F]Entity/Anim`）
- 说明：`AnimEvent` / `AnimAttackEvent` 都在 `Entity/Anim/` 下（架构说明标 `[F]` 的部分），按规范我不擅自改

### 4. 服务器场景（DedicateServer.unity）——【已完成】
- [x] `Launcher`（RSServerLauncher，自动 StartHost）、`EnvironmengManager`（EnvironmentManager，**对象名拼写有误，仅影响观感**）、`GameController`（Tool / NetworkManager / BattleManager / InfoManager / EnsCorrespondent / EnsSpawner 走共用预制体）
- [x] 地形 `LevelGraphic.prefab` 随场景加载（服务器侧只提供点位，不含贴图表现）
- [x] 不含 AssetsManager、不含 InputManager（符合架构要求）

### 5. 客户端场景（MainScene.unity）——【已完成】
- [x] Tool / NetworkManager / BattleManager / InfoManager / SaveManager / EnvironmentManager / CameraController / ClientLogicManager / InputManager / AssetsManager / UIManager + UIDocument
- [x] `Level.prefab` 随场景加载（地形 + 点位）
- [x] 字体已配（`Assets/Files/UI/Font/黑体.ttf`，默认字体无中文字形）

### 6. InfoManager 配置（服务器与客户端各一份）——【大部分完成，剩 3 项】
- [x] 角色属性：进攻方 18 → `AttackCharacterInfoList`；防守方 6 → `DefenseCharacterInfoList`
- [x] 其它实体属性：僵尸 1、精英僵尸 14、外围守护点、中心守护点、水晶、防御塔 4、瘟疫树
- [x] 服务器实体模板（只含组件无图形）：进攻 18 / 防守 6 / 僵尸 21 / 精英僵尸 14 / 守护点 2 / 水晶 12 / 防御塔 4 / 瘟疫树 1
- [ ] **`entity_layer`**：必须填专用层「Entity」的层号 **8**。填 0 会与地形等 Default 层 collider 混进同一查询缓冲（32 个），被占满后漏判，直接影响近战与子弹命中
- [ ] **`ground_layer`**：**必须给地形与所有可站立的平台/物件建/挂一个地面层并填入层号**。落地检测只查这一层；填 0 会退化为 Default 层，漏挂的单位会被判成永远悬空
- [ ] **`SkillInfoList`**：`Assets/Files/Info/Skill/` 下 31 个 SkillInfo 资产（id 0~30）**未被引用**，技能栏因此没有名称与图标；id 31~49、50~73、100~182 的资产尚未创建
- [ ] ⚠️ **实体预制体必须自带 `Collider`**：`BulletShootPos`（取 `GetComponentInChildren<Collider>().bounds`）与 `SetupBody` 的 `useGravity = true` 都依赖它——**缺了会空引用或一直坠落**（此项只能在编辑器内实测）
- [ ] ⚠️ **会移动的实体预制体必须挂 `EntityAnim`**：移动循环的判据是 `anim == null` 跳过，AI 的移动意图也靠 `OnTickMove` 每帧消费 → 缺了**单位不会动**（僵尸、AI 玩家都受影响）

### 7. 地形点位（LandscapeSpawns，挂在 `Level.prefab`，全项目唯一点位来源）——【已完成】
- [x] 锚点（Transform）：守护点 4（**前 3 为外围、第 4 及以后为中心**）、防御塔 14、瘟疫树 4、进攻方出生/复活 8、防守方出生/复活 2
- [x] 坐标（Vector3）：水晶 **2000** 个、僵尸 100 个 —— 水晶数量远超右键菜单生成时的默认值 100，请确认是有意为之
- [x] Gizmos 半径已调（`crystalGizmoRadius` 1.35 / `otherGizmoRadius` 1.14）
- [ ] **服务器场景烘焙 NavMesh**：僵尸 AI 与 AI 玩家都走 `NavMesh.CalculatePath` 寻路。**未烘焙不报错，但会静默回退成"直线朝目标走"** → 穿墙/穿地形。只需在**服务器场景**烘焙（烘焙产物属 `Assets/Files/**`，不在本仓）

### 8. AssetsManager（仅客户端）——【已完成，缺图标】
- [x] 角色图形：进攻 18 / 防守 6 / 僵尸 21 / 精英僵尸 14 / 水晶 12 / 防御塔 4 / 守护点 2 / 蘑菇 30
- [x] 武器：刀 11 / 长枪 8 / 枪械 15 / 魔法球 16（正好覆盖武器 id 0~49）
- [x] 特效：子弹 60 / 护盾 13 / 范围魔法 10 / 魔法阵 10 / Buff 31
- [ ] **图标资源未导入**：`Assets/Files/UI/` 下只有 Font 与 Shaders，技能/角色图标尚未放入工程，SkillInfo 的 icon 无从绑定

### 9. 联调验证 ——【待做】
- [ ] 先开服务器场景 → Play（日志出现 "Server Started"）
- [ ] 再开客户端场景（可另开一个编辑器实例）→ Play → 选攻/守角色 → 填服务器 IP（本机留空）→ 连接 → 组队大厅 → 选队、编辑双方 AI 数量 → 开始游戏
- [ ] 预期：连接成功 → 组队大厅（房间状态广播）→ 战斗页（底部技能栏、顶部时间/昼夜/分数、右侧守护点血量、头顶名字与血条）
- [ ] 把任何编译错误 / 运行报错贴给我
- ⚠️ **端到端联调从未跑通过**（RPC 尚未生成），所以 2.2 那类"配置未接"的问题还没被暴露出来

### 10. 打包时才需要
- [ ] 把 `Unlit/Color` 加入 Project Settings → Graphics → **Always Included Shaders**（否则头顶血条在 Player 里不显示；编辑器内正常）

---

## 二、代码侧现状

### 2.1 功能模块现状
| 模块 | 现状 | 待实现 |
| --- | --- | --- |
| **AI 行为** | 落点 = `EntityData.TickAI()`（虚方法）+ `BattleManager.UpdateAI()` 每帧遍历调用（各实体按 id 错峰）。**僵尸/精英僵尸**、**防御塔/瘟疫树**、**AI 虚拟玩家**三条链都已实现 | 已完成。后续新增 AI 单位覆写 `TickAI`（会移动的）或继承 `AutoCastEntityData`（固定周期施法的） |
| **电脑单位行为** | 僵尸/精英僵尸已由 `ZombieEntityData.TickAI` 驱动；**防御塔与瘟疫树已由 `AutoCastEntityData` 驱动**（每 `AutoCastInterval` 秒随机取一个技能释放，间隔由子类实现、当前各 5s；射程由技能 `CastRange` 把守） | 已完成。后续新增同类固定单位只需继承 `AutoCastEntityData` 并给出 `AutoCastInterval` |
| **寻路** | **已上提到 `EntityData`**（`MoveTo` / `StopMoving` / `ResolveNavDirection`，NavMesh 只算方向不用 NavMeshAgent），僵尸与 AI 玩家共用；真人玩家仍走"输入相对移动 + 渐转" | 已完成。**前置：服务器场景需烘焙 NavMesh**（见 §7） |
| **死亡表现** | 死亡即摧毁实体、复活重建；`ClientEvent.OnEntityDead` 定义了但没人触发 | 死亡表现细化（倒地/消失），以及该事件的触发与订阅 |

### 2.2 未接线（组件或接口已存在，但没人用）
| 位置 | 现状 |
| --- | --- |
| `ClientEvent.OnEnterWorld`(10201)、`OnExitWorld`(10203)、`OnEntityDead`(10220)、`OnLocalPlayerAliveChange`(10221)、`OnEntityDisplayCreate`(10232) | 常量已定义，**无人触发** |
| `ClientEvent.ShowConfirm`(10302) | 面板（`ConfirmUnit`）已建好，**既无人触发也无人订阅** → 确认对话框没接线 |
| `ClientEvent.ShowFloatingText`(10303) | 无人触发也无人订阅（飘字实际走 `BattlePage` 内部方法） |
| `ClientEvent.OnRightClickBlocked`(10304) | BattlePage 订阅了，**无人触发** → 该提示永远不会出现 |
| `ClientEvent.OnEntityDisplayRemove`(10231) | 有 2 处触发，**无人订阅** |
| `SCBattleEvent.Type.CrystalBroken`(=3) | 服务端 `CrystalEntityData` 有触发，**客户端 `BattlePage.OnBattleEvent` 的 switch 未处理** → 被蘑菇感染的水晶被摧毁时无任何反馈 |
| 攻占瘟疫树的飘字（`BattlePage.cs`） | 硬编码「攻占瘟疫树！**CD 加速**」——**与实际不符**：全项目不存在技能冷却恢复倍率机制（策划案 L154），实际奖励是「瘟疫祝福」（+75% 出伤 / −25% 受伤） |
| `TransitionManager` | 只在 `Tool` 里有静态字段，无任何转场逻辑 |
| `SaveManager.UnlockCharacter` | 无写入方（解锁实际走角色 SO 的 `unlockPlayerLevel` 自动判定） |
| `AssetsObjectPool` | **整池零调用**（只有 `Awake` 注册到 `Tool`）→ 架构说明承诺的"高复用内容池化"没落地，实体/武器/特效都在直接 `Instantiate` |
| `NoticeMessageMap` | 19 条文案只有 5 条真在用（13/14/15 武器相关、17/18 开局校验）；其余 14 条要么有硬编码替代、要么既无实现也无替代（含"三种结算原因"不提示） |
| `RandomMapper`、`ModSpace` | 零引用（未使用的工具类） |

### 2.3 数值待定稿（不是逻辑缺失，但影响可玩性）
- Config 的 4 个 region 明确标注"占位初值，待策划定稿"：通用 Buff 数值 / 防守方技能参数 / 技能参数补充 / 防守方被动数值
- 僵尸 AI 行为参数为占位初值（`Config` 僵尸 region）：最大追击距离 16m、游荡半径 8m / 停顿 2s、索敌半径 12m、决策间隔 0.15s；爪击 2m 与嘶吼 10m 两个分界由你给定
- **玩家技能 `CastRange` 已批量补齐但数值为占位**（`SkillBase.CastRange`，71 个技能类里 48 个配了值、23 个保持 0）：武器分组 刀 2m / 长枪 8m / 枪械 18m / 魔法球 15m（`SkillFrostNova` 按真实半径给 3、`SkillSweepPole` 是手部球给 2）；**防守方 50~73 的 18 个技能全部为占位待定**（`SkillEyeMark`/`SkillMireCast`/`SkillDetonatePlague`/`SkillFogCast` 是全场技能，无距离概念故不加门闸）。**注意**：不需要目标的技能（自身增益/召唤/友方护盾/全场效果，共 23 个）**必须保持 CastRange = 0**，给了正数会导致"周围没敌人时放不出来"
- AI 虚拟玩家的行为参数为占位初值（`PlayerAiController` 内常量）：决策 0.15s、重选目标 1s、最大追击 40m、接敌距离兜底 12m、撤退阈值 30% / 退 25m / 最长 4s
- 79 个技能的倍率 / 判定半径 / 弹道时长同样为占位；`initial_skills` 进攻方 18 人分配为临时值
- 头顶血条的表现参数（宽高、偏移、颜色）为占位，集中在 `LabelPlayerManager` 顶部常量区

### 2.4 代码卫生（可顺手清理，不影响功能）
- 僵尸分桶（`ChunkSearcher.Zombies`）**索引不更新**：只在增删时维护，`TickMovement` 只同步 `Entities`。僵尸 AI 与 AI 玩家的索敌走的都是 `Entities` 桶（位置每帧同步），故目前无害；**将来若要直接遍历 `Zombies` / `Towers` / `Crystals` / `Beacons` 桶做范围查询，必须先接线 `UpdateObjectPosition` / `RefreshAll`**
- 3 个非法 meta guid（`Entity.meta`、`Entity/Attribute.meta`、`Entity/EntityComponents.meta`，56 字符 base64 应为 32 位 hex）待修；已核实无任何外部引用
- **`Vector3Serializer` 字节检查不一致（在 EnsNetcode 库仓，不在本仓）**：注释写"x/y/z 各 double 8 → 共 24 字节"，实际用的是 `FloatSerializer`（各 4 字节 = 12 字节）；`Serialize` 检查 `< 24`、`Deserialize` 检查 `< 12`。**后果**：缓冲剩余 12~23 字节时 `Serialize` 会返回 false，而调用方忽略返回值 → **该 Vector3 静默不写**。建议把 `Serialize` 的 24 改为 12
- `EntityAnim.CharcterAnimType` 拼写错误（应为 `CharacterAnimType`），改名会影响资产，建议保留
- `SCBattleEvent.Type` 内嵌类名易与 `System.Type` 混淆
- 重复资产：`Assets/Files/Anim/CharacterAnim 1.controller`（30 个状态、无行为脚本、**无人引用**）可删
- 服务器场景对象名 `EnvironmengManager` 拼写错（仅影响观感）

### 2.5 已完成（不用再动）
- **AI 虚拟玩家**（`PlayerAiController`：Approach/Engage/Retreat 三态、就近打最近的敌对目标、攻守共用一套无分叉）＋**全部玩家技能补 `CastRange` 施法距离门闸**（71 类中 48 类配值、23 类"不需要目标"保持 0）＋**寻路上提到 `EntityData`**（僵尸与 AI 玩家共用 `MoveTo`/`ResolveNavDirection`）＋**`EntityData.aiControlled` 标记**
- **僵尸 AI**（`ZombieEntityData.TickAI`：游荡→追击→攻击的距离分档、左右爪击交替、锁敌不换目标）＋**自动施法基类 `AutoCastEntityData`**（防御塔/瘟疫树继承：每 `AutoCastInterval` 秒随机取一个技能释放，间隔由子类实现）＋**僵尸独立成 `Zombie` 单向阵营**（只敌视进攻方，只有进攻方敌视它）＋**`SkillBase.CastRange` 施法距离门闸**（爪击 2m / 塔 20m / 树 8m，由 `TryUseSkill` 统一校验）
- 技能池 **79 个技能类全部实现并注册**（水晶武器 0~49 / 防守方 18 / 非玩家 8 / 空手 180~182）
- Buff 系统：双轨属性 + 查询式战斗管线（护盾→减伤→受伤乘区→扣血→反伤）、强控打断位移与置零动画速度、破霸体、DoT/光环 tick
- 子弹容器：时间戳推进、穿透（同一发对同一目标只结算一次）、上一帧→当前帧胶囊覆盖路径防穿模；`Damageable` 伤害窗口机制已接线
- 战斗规则：世界生成、水晶定时重生、夜间僵尸刷新、权威移动、死亡复活与愈战愈勇、守护点分层减伤、分数制胜负、对局结算面板、视野双层可见性 + 阵营共享小地图、防守方三类被动
- 僵尸寻路 `MoveTo/Stop`、每人击杀数 `KillCountByClient`、加载界面接线、`Tool` 启动自检、区块索引修复（`_chunkOf` + 每帧同步）、刚体不锁 Y 轴、瘟疫树攻占、死灵漫步移速、`Timer` 统一延时入口、速度来源收口
- 客户端逻辑层：`ClientLogicManager` 总控 + 5 个子管理器
- 存档：双等级制数据、升级经验公式、角色解锁
- **按策划案 [A] 明确不做**：匹配功能、PVP/PVE 队列、自动 AI 补位、自动阵营分配（房间内自由编辑双方 AI 数量、手动选阵营）

---

## 三、约定与备忘（写后续代码时遵守）
- 服务器↔客户端传输的类：`SC` 开头（服务器→客户端）/ `CS` 开头（客户端→服务器），序列化器写在同一文件内
- ScriptableObject 一律 `Info` 结尾
- 一个类内不影响理解的计算函数放最后，用 `#region//Local` 包裹
- 全局参数被动（复活速度、僵尸刷新等级等）开战一次性计算，不做成 Buff
- 客户端只做表现，关键逻辑全部服务器计算
- **客户端逻辑一律进 `ClientLogicManager` 的子管理器**（普通 C# 类，继承 `ClientSubManager`）：能并入既有子管理器就别新建；UI 的数据来源仍是 `ClientEvent` 事件
- 事件订阅与解绑必须成对（`EventManager` 是静态的，只订阅不解绑会随场景重载累积）
- 服务器与客户端都需要的逻辑（如轨迹构建）写成双端共用的函数，不要各写一份
- **延时 / 重复调用一律用 `Timer.AddTimer`**（`Utils/MonoUtils/Timer.cs`，由 `Tool` 每帧驱动），禁止自己写计时字段
- 注释只解释"为什么"，不复述代码、不写变更历史，禁止大段注释
- **移动 / 速度的完整口径见《代码架构说明》"速度"节**（允许的五个来源 + 动画机地位相同）：游戏逻辑只触发动画与传参数，**不自己产生速度**
- **AI 的落点 = 覆写 `EntityData.TickAI()`**（虚方法，`BattleManager.UpdateAI` 每帧遍历调用；**不要往 `BattleManager` 里加类别分支**）。三条链：会移动/追击的 `ZombieEntityData`；固定周期施法的继承 `AutoCastEntityData`（每 `AutoCastInterval` 秒随机放一个技能）；玩家 AI 走 `PlayerEntityData.TickAI` → `PlayerAiController`（**攻守共用、不分叉**——目标选择只问"最近的敌对目标"，差异由 `HostileOf` 承担）。寻路**只借 NavMesh 算"往哪走"**，**不用 `NavMeshAgent`**（它会自己驱动 transform/速度），只用 `NavMesh.SamplePosition` + `CalculatePath` 这类"只算不动"的 API；工程装的是旧版 Navigation，**只需在服务器场景烘焙**，烘焙产物属 `Assets/Files/**`
- **`Entity/Anim/**` 动画模块由你自行开发，我不改动**
- ⚠️ **`Assets/Files/**` 与场景/预制体不在本仓内**（本仓 = `Assets/RevelryOfSpecular`）→ 这些改动**无法提交、不受版本控制**
