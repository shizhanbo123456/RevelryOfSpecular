# 搭建与待办清单（当前状态）

> 本清单列出需要你在 Unity 编辑器中完成的事（代码无法自动完成的部分），以及当前代码留空 / 未接线的模块。
> 已按 2026-09-14 的代码与工程实际状态逐条核对更新；2026-09-20 做了完整复核；**2026-09-22 修正了第零节的误判并补入阵营系统重构**；**2026-09-23 复核：僵尸寻路 / 每人击杀数 / Timer 取代 GenericTimer / 加载界面接线落地，并更正了「技能缺延时能力」「TryGetAnimEvent 无调用」两处旧结论**；**2026-09-24 复核：ENS 库移出本仓并放到 `Assets/` 下（第 1 节已按新位置重写）、旧位置残留目录已删、生成物已删；速度来源收口（新增 0.12）；动画侧速度声明已接通；「所有动画机地位相同、不分主次」口径入档**。

---

## 零、2026-09-20 复核新增（优先级最高）

### 0.1 ~~🔴 服务器实体模板全部缺 `EntityData`~~ —— **误判，已作废（2026-09-22 更正）**
原文称"模板 `EntityData=0` → `SpawnEntity` 返回 0 → 开战 24 个实体一个都刷不出来"，**与代码不符**：
- `EntityData` **本来就不挂预制体**，由 `BattleManager.SpawnEntity`（`BattleManager.cs:192-200`）在生成时按类别运行时补：
  `if (data == null) data = EntityData.AddTo(go, type.category);` → `AddComponent<PlayerEntityData / ZombieEntityData / BeaconEntityData / CrystalEntityData / TowerEntityData / PlagueTreeEntityData>`（`EntityData.cs:97-123`）。代码注释已写明原因："模板与客户端图形是同一批预制体，挂上去客户端表现体也会带上 EntityData"。
- `Rigidbody` 同样运行时补（`SetupBody`，`EntityData.cs:543-548`）。
- `SpawnEntity` 唯一的 `return 0` 分支是 **`type.category` 没有对应 `EntityData` 子类**（当前只有 `EntityCategory.Prop` 无子类），角色/僵尸/守护点/水晶/塔/树都有子类，**不会因此返回 0**。

仍**需要你在 Unity 里确认**的（代码侧只能看到依赖，看不到资产）：
- 实体自带 **`Collider`**：`EntityData.cs:470` 的 `GetComponentInChildren<Collider>().bounds`（`BulletShootPos`，无武器单位远程发射用）与 `SetupBody` 的 `useGravity = true` 都依赖它；**缺 Collider 会 NPE / 一直坠落**。此项以你编辑器内实测为准。
- ⚠️ `Assets/Files/**` **不在任何 git 仓库内**（只有 `Assets/Scripts` 是仓库）→ 模板/预制体/地形的改动**无法提交、不受版本控制**（此条仍然成立）

### 0.2 ✅ ~~`ChunkSearcher` 区块索引永不更新~~ —— **已修复（2026-09-23，代码侧）**
原状：`ChunkSearcher.UpdateObjectPosition` 与 `RefreshAll` **零调用**；`GetIdsInRange` 只查区块桶，而 `Entities.Add` 只在出生时调用一次 → **实体离开出生区块后，范围查询再也查不到它**（`GetNearestEnemy` / `GetNearestInCamp` 是技能自动索敌 `SkillBase.AimPos` 的必经之路）。

修复含**两处**，缺一不可：
1. **`UpdateObjectPosition` 本身是坏的**：它把 `_getPosition(obj)` 读了两次当成"旧位置 / 新位置"，两个区块永远相等 → **即使调用也是空操作**。已新增 `_chunkOf`（id → 当前区块）记录真实所在区块，`Add` / `Remove` / `Clear` / `RefreshAll` / `UpdateObjectPosition` 全部维护它；顺带修掉 `Remove` 按当前位置反推、导致旧区块残留脏 id 的问题。
2. **无人调用**：在 `BattleManagerCombat.TickMovement` 的既有循环里（本来就只遍历可移动单位）追加 `EntityContainer.Entities.UpdateObjectPosition(e.id)`，无额外遍历开销。

`Zombies` / `Beacons` / `Crystals` / `Towers` 四个分桶同为 `ChunkSearcher`，目前**没有任何范围查询**（只用 `Count` 与 `foreach`）；`Beacons`/`Crystals`/`Towers` 内的实体不移动，僵尸分桶的索引会随移动变旧 —— 若后续要对僵尸做范围查询，需在同一处一并同步 `Zombies`。

### 0.3 🟠 与策划案不符（代码侧）
- ~~**瘟疫树索敌阵营错**~~ —— **已修复（2026-09-22）**：阵营改为 `[Flags]` + `EntityCampUtil.HostileOf`，中立（瘟疫树）的敌对掩码 = {攻, 守}，不再只索敌其它中立。详见第零节 0.7。
- **死灵漫步缺"速度提升"**：策划案 124/313 行要求"自身速度提升 + 绝对霸体 + 周围每秒伤害"，代码只有绝对霸体（`HasSuperArmor`）+ 光环 DoT。

### 0.4 ~~⏳ 待确认：两个 0 字节的新脚本~~ —— **已解决（2026-09-22）**
`Entity/Anim/AnimHitEvent.cs`、`AnimMotionEvent.cs`（以及 `AnimSpawnEvent.cs`）均已实现为非空内容，都是 `AnimEvent` 的薄子类（各自只覆写 `State` 为 Hit / Motion / Spawn），供动画状态机补挂用。

### 0.7 ✅ 阵营系统重构（2026-09-22 完成，代码侧）
- `EntityCamp` 改为 **`[Flags]`**：`None=0 / Attack=1 / Defense=2 / Neutral=4 / Prop=8`
- 同文件新增 `EntityCampUtil`：`HostileOf(camp)` = **除自身外全部阵营；若自身是中立或 Prop 再剔除中立与 Prop**
  → 攻 = {守, 中立, Prop}｜守 = {攻, 中立, Prop}｜中立 = {攻, 守}｜Prop = {攻, 守}
- **水晶阵营 `Neutral` → `Prop`**（`BattleManagerWorld` 两处生成 + 重生）
- 攻击判定统一改走敌对掩码，取代原来的"非同阵营即命中"：子弹命中（`BattleManagerCombat.TryHitBullet`）、近战球判定（`SkillBase.StrikeSphere`）、范围索敌（`BattleManager.GetNearestEnemy/GetNearestInCamp/GetAllInCamp` 改掩码制）、范围效果（`SkillBase.AllInCamp/EnemiesIn`、死灵漫步光环）
- 删除 `BattleManager.OppositeCamp` 与 `SkillBase.Opposing`（统一收口到 `EntityCampUtil`）
- 对攻/守而言新掩码与旧的"非同阵营"**完全等价**（行为不变）；真正变化的是中立：不再索敌中立/Prop
- ⚠️ 连带变化见 `代码架构说明.md`「阵营」节末尾的说明

### 0.5 仓库卫生
- 文件夹 `.meta` 的**非法 guid（56 字符 base64，应为 32 位 hex）**：原有 8 个，**2026-09-23 已修 7 个**（`Editor.meta`、`EnsNetcode.meta`、`GameController.meta`、`Server.meta`、`Transport.meta`、`UI.meta`、`Utils.meta`，见提交 `06d1a5d`）；**剩 3 个未修**：`Entity.meta`、`Entity/Attribute.meta`、`Entity/EntityComponents.meta`。已核实这 3 个 guid **没有任何外部引用**（全 `Assets/` 只在这 3 个 meta 自身出现）→ 与前面 7 个同样安全，可用同样方式（把 guid 换成合法 32 位 hex）修正，随时可做
  - 注（2026-09-24）：已修的那 7 个里，**`EnsNetcode.meta` 已随库移出本仓一并删除**（旧位置残留的空目录 + meta，guid `62fc9419d808ed549a458b0d2b80e9a2`，核实无任何引用）
- 此前 3 个未跟踪的 `.meta`（`Bullet/FollowTrajectory`、`Entity/Attribute/PlayerCharacterInfo`、`Transport/SCDayNightInfo`）**已补齐并纳入跟踪** ✓

### 0.6 已修正的两条过期结论
- `EffectType.MinimapLost` **已接线**（`BattleManagerVision.ApplyMinimapLostOnSpawn` / `SetMinimapLost`），之前"无施加点"的说法作废
- 非玩家单位 `SetSkillList` **已接线**（`BattleManager.cs:202`，走 `Config.GetInitialSkills`）

### 0.8 ✅ 瘟疫树攻占 + 死灵漫步移速（2026-09-22 完成，代码侧）
- **瘟疫树攻占**（策划案 6.2 已定稿）：`PlagueTreeEntityData` 实现「被打死即攻占」——
  - 归属 = 把树**血量清零那一次攻击的攻击者** → 挂 `EffectType.PlagueBless`（+75% 出伤 / −25% 受伤，30s，`Config.plague_bless_*`）
  - **被 Buff/DoT（固定数值伤害）打死的没有归属**，不给任何实体加成
  - 树不属于任何一方；死时广播 `SCBattleEvent.PlagueTreeCaptured`（客户端表现**尚未接线**）
  - 刷新时序：开战后 30s 刷第 1 棵，被打死后 60s 倒计时重生（`BattleManagerWorld.plagueTreeRespawnTime` + `SpawnPlagueTree`）
  - **注意**：树**主动攻击**仍未实现（同僵尸/塔，等 AI；策划案 6.2 要求"谁靠近打谁"）
- **死灵漫步移速提升**：`EffectType.DeathStroll` 已纳入动画播放速度通道（`GetMoveAnimSpeedMultiplier` / 客户端 `ComputeMoveAnimSpeedMultiplier` / `IsMoveSpeedEffect`），倍率 `Config.death_stroll_speed_up`（**占位 1.3，待策划定稿**）。与「加速」并存时**连乘**。

### 0.9 ✅ 刚体旋转不再锁 Y 轴（2026-09-23 完成，代码侧）
`EntityData.SetupBody` 由 `FreezeRotation`（三轴全锁）改为 **`FreezeRotationX | FreezeRotationZ`** —— 只防倒地翻滚，**Y 轴旋转不再锁定**。`Config.rb_angular_drag` 因此对 Y 轴生效（`SetVelocityForward` 等速度通道不受影响）。

### 0.10 ⏸️ AI 全线统一留空（2026-09-23 确认）
僵尸 / 精英僵尸 / 防御塔 / 瘟疫树的**决策与施法**暂时全部不做，等操控方式定下来再统一做（`BattleManager.UpdateAI` 保持空方法）。
**注意：移动能力已经有了** —— 僵尸寻路 `ZombieEntityData.MoveTo(Vector3) / Stop()` 已实现（见 0.11），只是**目前没有任何调用方**（AI 决策层还空着），所以刷出来的僵尸依旧不动。
已确认的后续方案备忘：僵尸移动**只借 NavMesh 算"往哪走"**，实际移动仍走"调整朝向 + 动画声明速度"；因此**不能用 `NavMeshAgent`**（它会自己驱动 transform/速度），要用 `NavMesh.SamplePosition` + `NavMesh.CalculatePath` 这类只算不动的 API。工程装的是**旧版 Navigation（非 `com.unity.ai.navigation` 的 NavMeshSurface）**，并且**只需在服务器场景烘焙 NavMesh**（服务器权威，客户端只表现）。⚠️ 烘焙产物属 `Assets/Files/**`/场景数据，**不在 git 仓库内，无法提交**。
另：`Entity/Anim/**` 动画模块由用户自行开发，**不要改动**。

### 0.11 ✅ 2026-09-23 新增落地（代码侧）
- **僵尸寻路**：`ZombieEntityData` 不再是空类 —— 新增 `MoveTo(Vector3 dest)` / `Stop()`，逐帧推进写在 `OnTickMove` 覆盖里（`BattleManagerCombat.TickMovement` 本来就驱动 `Zombie/EliteZombie`；可移动判据已改为"挂了 `EntityAnim`"，见《代码架构说明》速度节⑫）。用 `NavMesh.SamplePosition` 吸附目标 + `NavMesh.CalculatePath` 取拐点算方向，算不出路径时直线兜底；朝向按 `Config.move_turn_rate` 渐转并把角速度交给 `YawSpeed`（客户端推演）；**不写速度、不碰 position**，落地仍走 `SetMoveInput` + `anim.Move` 同一条链路。**无调用方（等 AI 决策层）**
- **每人击杀数**：`BattleManagerCombat.KillCountByClient`（按 clientId）。口径 = **仅击杀敌对阵营的玩家角色**（僵尸/防御塔/水晶/守护点不计），归属走 `lastAttacker` → `EntityOwnerClient`，`ClearBattleState()` 一并清空。**暂不下发客户端**（要显示结算战绩需再扩协议）。每人死亡数仍为 `ReviveState.deathCount`
- **延时统一入口**：旧的 `GenericTimer` 已被重写为 **`Timer`**（`Utils/MonoUtils/Timer.cs`，签名与语义见《代码架构说明》Utils 节与技能节）。**技能侧不缺延时能力** —— 多段/延时结算一律用 `Timer.AddTimer`，不要再找"延时模块"
- **清理过时注释**：删掉 `EntityData.OnKilled` 里"TODO: 掉落/击杀事件/愈战愈勇等"（这三件事都已在 BattleManager：掉落 = `TryDropCrystalWeapon`、复活叠层 = `RespawnPlayer`、击杀计数 = `KillCountByClient`/`DefenseKills`）

### 0.12 ✅ 2026-09-24 速度来源收口（代码侧）
- **口径**（用户明确）：动画模块之外的代码**只能控制三种速度** —— ① 摩擦力自然衰减、② 技能等特殊效果带来的位移、③ 重力；**玩家主动操控的速度全部且只能来自动画模块的声明**。
- **已删**：`EntityData.moveSpeed`（原"退化移速"，按 `EntityAnimData.legHeight × 2.6` 换算）、`Config.base_move_speed`、`Config.jump_speed`（竖直初速已由 `AnimJumpStartEvent` → `animData.jumpSpeed` 声明）。`ResolveMoveVelocity` 由四种情况收成三种（**不再有"动画没声明就按输入给速度"这条路**），并去掉因此失效的 `canInput` 参数。
- **现在允许的水平/垂直速度来源共五种**（详见《代码架构说明》速度节开头）：动画声明 / 地面摩擦衰减 / MotionBase 位移 / 重力 / **击飞**（击飞经确认为**独立来源**，不走 MotionBase，代码保留）。
- **移速"快慢"完全通过动画播放速度实现**：加速/减速/泥沼/死灵漫步只改 `EntityAnim.PlaybackSpeed`（既写 `animator.speed`、也乘进动画声明的速度值），不存在第二条速度通道。
- ✅ **动画侧已接上（2026-09-24 提交 `222dacd`「动画接入速度控制」+ `15cdc12`「校正动画和速度关系」）**：`AnimMotionEvent.VelocityX/VelocityY` 已删除，改由各状态**直接调** `anim.SetVelocityForward/Vertical`；`AnimEvent` 提供 `MoveSpeed`/`JumpSpeed`（读 `EntityAnimData.RunSpeed`/`JumpSpeed`）；`AnimRunEvent`/`AnimJumpStartEvent`/`AnimJumpEndEvent`/`AnimRollEvent` 均已声明速度。**速度声明链已通**，剩下的只是资产侧"挂状态 + 配参数"（见第 3 节）。
- **速度声明的唯一发出点是动画机状态的行为脚本**；游戏逻辑只触发动画与传参数，不自己产生速度（唯一例外：击飞）。**所有动画机地位一律相同**（见《代码架构说明》`[F]Entity/Anim` 一章）。
- 顺带：`EntityAnimData.LegHeightToStandartRunSpeed` 已被 `RunSpeed` 取代（`runSpeed` 表达式相同，二选一即可）。

---

## 一、待你在 Unity 里完成

### 1. 生成网络代码 ——【暂缓，代码未完成前不做】
- 用 Unity 2022.3.62 打开项目 `RevelryOfSepulcher`
- 菜单栏 **Ens - Generate Code**，生成 `Gen/NetworkManager.Generated.cs`（在库目录下）
  - 16 个 `[Rpc]`（6 上行 + 10 下行）全部依赖它；未生成则所有网络调用无效（**注意：不会编译报错** —— `EnsBehaviour.CallFuncRpc` 有兜底重载，只 `LogUnknownFunc()`，联机层会静默失效）
- 📍 **库已移出本仓（2026-09-24 更新）**：`EnsNetcode` 库现在放在工程里、**与 `RevelryOfSpecular` 平级**（本机路径 `Assets/ENS-Netcode`），不再嵌套在本仓内；`Assets/RevelryOfSpecular/EnsNetcode/` 这个旧位置（空目录 + 残留 meta）**已删除**。库仍保留自己的 git 仓与远程。
- ⚠️ **2026-09-24 复核：生成器两个常量都要对齐** —— `Editor/CodeGenerator.cs:13` 的 `genDir` 写的是 `Assets/EnsNetcode/Gen`，而库实际目录名是 `ENS-Netcode`（少了中划线、大小写也不同）；`:12` 的 `sourceDir` 仍是 `Assets/Scripts`，本仓脚本实际位于 `Assets/RevelryOfSpecular/**`。**点生成前必须先对齐这两个常量，否则会写到不存在的错误目录。**
- ✅ **2026-09-24 更新：`Gen/NetworkManager.Generated.cs` 已按用户要求删除**（"生成代码最后再做，项目里完全不留任何生成的代码"）→ 全 `Assets/` 下已无任何 `*Generated*.cs`，库内 `Gen/` 现为空目录。故 **RPC 层当前处于失效状态**（未生成不报编译错）。
- ⚠️ 库仓因此压着一个**未提交的删除**（`D Gen/NetworkManager.Generated.cs`），是否提交由用户定。
- ⚠️ **2026-09-22 确认：代码尚未完成，此时生成网络代码没有意义，先不处理**（原文"必须第一步"作废）
- 待逻辑收尾后再生成，届时把 Console 报错反馈给我

### 2. 客户端场景：删除 ClientDisplayManager 组件 ——【待做】
- [ ] `MainScene.unity` 上挂着 `ClientDisplayManager` 的对象，**把该组件删掉**（该类已改为普通 C# 类，不再挂场景，会显示为 Missing (Mono Script)）
- 不需要动的：`ClientLogicManager` 仍是 MonoBehaviour 且已挂在场景上；其它 5 个子管理器由它在运行时创建，不会出现在 Inspector 里

### 3. 动画状态机补挂 AnimEvent ——【待做，优先级高】
`Assets/Files/Anim/CharacterAnim.controller` 共 **33 个状态**，其中：
- **14 个攻击状态**已挂 `AnimAttackEvent`（Attack_Kick_A_Root / Attack_Hand_1R / Attack_Hand_2L / TZ_aggresive_scream / TZ_aggresive_attack_A / TZ_aggresive_attack_B / WpAttack_Hand_1R / WpAttack_Hand_2L 0 / WpAttack_Hand_All / JAttack_AirFront_All / JAttack_AirDrop_All / mega_short / mega_middle / mega_long）
- **0 个状态挂 `AnimEvent`**；其余 **19 个状态一个行为脚本都没有**

代码是**纯推送制**：`EntityAnim.CurrentState` 与 `currentAnimId` **只由 `AnimEvent.OnStateEnter` 更新**（`Do*` 方法只写状态机参数，不直接改状态）。因此这 19 个状态会引发两个问题：
1. **动画片段不会同步给客户端** —— 下发的是 `currentAnimId`，客户端按 hash 播放；这些状态不进这个值，客户端会停在最后一次攻击动画上（走/跑/跳/滑铲/受击/死亡都不对）
2. **霸体等级取错** —— `EntityData.GetEndureLevel()` 读 `anim.CurrentState`，状态不更新就会按旧状态判霸体，影响破霸体与受击

- [ ] 给需要同步的状态各自挂 `AnimEvent`，并在 Inspector 里把 `state` 枚举设对（Spawn / Motion / Attack / Hit / Die）
- [ ] 顺带确认 14 个攻击状态的 `AnimAttackEvent` 参数：`type`（AttackType）与 `threshold`（伤害判定发生在动作的归一化进度），双段攻击还要设 `useHit2` / `threshold2`
- [ ] ✅ **行为脚本侧已接好（2026-09-24，提交 `222dacd`/`15cdc12`），现在只差"挂到状态上 + 配参数"** —— 速度声明已落地：`AnimRunEvent` → `SetVelocityForward(RunSpeed × speedFactor)`、`AnimJumpStartEvent` → 跳跃竖直初速、`AnimJumpEndEvent` → 落地后水平位移、`AnimRollEvent` → `speedCurve × RunSpeed`。需要你在 Inspector 逐个配的是：
  - `AnimRunEvent.speedFactor`（逐状态倍率，走/跑靠它区分，代码已乘上）
  - `AnimJumpEndEvent.moveAfterLand`（**默认 false = 落地即停**；要落地后继续走就勾上）
  - `AnimRollEvent.speedCurve`（**不配 = 空曲线 = 翻滚零位移**）
  - `AnimAttackEvent` 的 `type` / `threshold` / `useHit2` / `threshold2`（同 116/117 条）
- [ ] ⚠️ **所有动画机地位相同、不分主次**：实体上的每个 Animator 都按同一套规则处理，需要同步状态 / 声明速度的状态，**每个动画机各自挂行为脚本，不要只挂在某一个上，也不要区分"主/子"**（详见《代码架构说明》`[F]Entity/Anim` 一章）
- 说明：`AnimEvent` 与 `AnimAttackEvent` 都在 `Entity/Anim/` 下；这是动画资产（架构说明里标 `[F]` 的部分），按规范我先不动，需要我改请明确说

### 4. 服务器场景（DedicateServer.unity）——【已完成】
- [x] `Launcher`（RSServerLauncher，自动 StartHost）、`EnvironmengManager`（EnvironmentManager，**对象名拼写有误，仅影响观感**）、`GameController`（Tool / NetworkManager / BattleManager / InfoManager / EnsCorrespondent / EnsSpawner 走共用预制体）
- [x] 地形 `LevelGraphic.prefab` 随场景加载（服务器侧只提供点位，不含贴图表现）
- [x] 架构要求已符合：服务器场景**不含 AssetsManager**、不含 InputManager

### 5. 客户端场景（MainScene.unity）——【已完成】
- [x] Tool / NetworkManager / BattleManager / InfoManager / SaveManager / EnvironmentManager / CameraController / ClientLogicManager / InputManager / AssetsManager / UIManager + UIDocument
- [x] `Level.prefab` 随场景加载（地形 + 点位）
- [x] 字体已配（`Assets/Files/UI/Font/黑体.ttf`，默认字体无中文字形）

### 6. InfoManager 配置（服务器与客户端各一份）——【大部分完成，剩 3 项】
- [x] 角色属性：进攻方 18 → `AttackCharacterInfoList`；防守方 6 → `DefenseCharacterInfoList`
- [x] 其它实体属性：僵尸 1、精英僵尸 14、外围守护点、中心守护点、水晶、防御塔 4、瘟疫树
- [x] 服务器实体模板（只含组件无图形）：进攻 18 / 防守 6 / 僵尸 21 / 精英僵尸 14 / 守护点 2 / 水晶 12 / 防御塔 4 / 瘟疫树 1
- [ ] **`entity_layer` 未配置**：预制体里没有这个字段，运行时取默认值 0。**必须填专用层「Entity」的层号 8**——掩码按它左移生成，填 0 会与地形等 Default 层 collider 混在一起，查询缓冲（32 个）被占满后会漏判，直接影响近战与子弹命中
- [ ] **`ground_layer` 未配置**（2026-09-24 新增）：落地检测（`EntityData.UpdateGrounded`）只查这一层，填 0 会退化为 Default 层。**必须给地形与所有可站立的平台/物件建/挂一个地面层并填入层号**，漏挂的单位会被判成永远悬空（表现为空中不掉速、`InAir` 一直是 true）
- [ ] **`SkillInfoList` 为空**：`Assets/Files/Info/Skill/` 下有 31 个 SkillInfo 资产（id 0~30）**未被引用**，技能栏因此没有名称与图标；id 31~49、50~73、100~182 的资产尚未创建

### 7. 地形点位（LandscapeSpawns，挂在 `Level.prefab`，全项目唯一点位来源）——【已完成】
- [x] 锚点（Transform）：守护点 4（**前 3 为外围、第 4 及以后为中心**）、防御塔 14、瘟疫树 4、进攻方出生/复活 8、防守方出生/复活 2
- [x] 坐标（Vector3）：水晶 **2000** 个、僵尸 100 个
  - 注意：水晶数量远超右键菜单生成时的默认值 100，请确认是有意为之
- [x] Gizmos 半径已调（`crystalGizmoRadius` 1.35 / `otherGizmoRadius` 1.14）

### 8. AssetsManager（仅客户端）——【已完成，缺图标】
- [x] 角色图形：进攻 18 / 防守 6 / 僵尸 21 / 精英僵尸 14 / 水晶 12 / 防御塔 4 / 守护点 2 / 蘑菇 30
- [x] 武器：刀 11 / 长枪 8 / 枪械 15 / 魔法球 16（正好覆盖武器 id 0~49）
- [x] 特效：子弹 60 / 护盾 13 / 范围魔法 10 / 魔法阵 10 / Buff 31
- [ ] **图标资源未导入**：`Assets/Files/UI/` 下只有 Font 与 Shaders，技能/角色图标尚未放入工程，SkillInfo 里的 icon 也就无从绑定

### 9. 联调验证 ——【待做】
- [ ] 先开服务器场景 → Play（日志出现 "Server Started"）
- [ ] 再开客户端场景（可另开一个编辑器实例）→ Play → 初始界面选攻/守角色 → 填服务器 IP（本机留空）→ 连接 → 组队大厅 → 选队、编辑双方 AI 数量 → 开始游戏
- [ ] 预期：连接成功 → 组队大厅（房间状态广播）→ 战斗页（底部技能栏、顶部时间/昼夜/分数、右侧守护点血量、头顶名字与血条）
- [ ] 把任何编译错误/运行报错贴给我
- 说明：**端到端联调从未跑通过**（RPC 尚未生成），所以上面第 3 条那类"配置未接"的问题不会被暴露出来

### 10. 打包时才需要
- [ ] 把 `Unlit/Color` 加入 Project Settings → Graphics → **Always Included Shaders**（否则头顶血条在 Player 里不显示；编辑器内正常）

---

## 二、代码留空待实现的模块（已按实际状态核对）

### 2.1 逻辑缺失（功能确实没写）

| 模块 | 现状 | 待实现 |
| --- | --- | --- |
| **AI 行为** | `BattleManager.UpdateAI()` 是**空方法**。AI 虚拟玩家已能按房间配置的数量创建（`RebuildAIPlayers`），但不会做任何事 | 决策层：索敌时机、触发间隔、移动、何时攻击/放技能。`BattleManager.cs:671` 的注释描述了预期行为（"有可用技能攻击最近单位，否则站立；被攻击逃跑"），代码未实现 |
| **电脑单位行为** | 僵尸/精英僵尸/防御塔/瘟疫树共 **8 个技能已注册却无任何调用方**（`TryUseSkill` 只被玩家输入路径调用） | 与 AI 同源的驱动层。僵尸刷新机制本身已实现（夜晚进度、数量上限），只是刷出来不会动 |
| **寻路** | **僵尸侧已实现（2026-09-23，见 0.11）**：`ZombieEntityData.MoveTo/Stop`，NavMesh 只算方向；其它单位仍只有"角色相对移动 + 渐转"的直线积分 | 让 AI 决策层调用 `MoveTo` 即可（能力已具备）；玩家 AI 若要寻路，可把这套逻辑上提到 `EntityData` 复用 |
| **死亡表现** | 死亡即摧毁实体、复活重建；`ClientEvent.OnEntityDead` 定义了但没人触发 | 死亡表现细化（倒地/消失等），以及该事件的触发与订阅 |

### 2.2 接线缺失（组件或接口已存在，但没人用）

| 位置 | 现状 |
| --- | --- |
| `EntityAnim.TryGetAnimEvent` | ~~接线缺失~~ **2026-09-23 更正：不是缺口**。动画同步是"一推一读"结构，推送侧早已接通：`EntityAnim.cs:111` 用 `GetBehaviours<AnimEvent>()` 取到各状态的 AnimEvent 并逐个 `Init`，`AnimEvent.OnStateEnter → EntityAnim.NotifyStateEnter` 会更新 `CurrentState`/`animId`；`TryGetAnimEvent` 只是备用的读接口，无人调用属正常 |
| `ClientEvent.OnEnterWorld`(10201)、`OnExitWorld`(10203)、`OnEntityDead`(10220)、`OnLocalPlayerAliveChange`(10221)、`OnEntityDisplayCreate`(10232) | 常量已定义，**无人触发**（死事件） |
| `ClientEvent.ShowLoading`(10301) | ~~加载界面没接线~~ **已接线（2026-09-23）**：`HomePage.OnConnectClicked` 在 `TryConnect` 前后成对显示/隐藏遮罩（连接自旋最长 5+5 秒，是客户端唯一的真实长等待）；`UIManager.ShowLoading` 增加 `(bool, string)` 重载以复用 `LoadingUnit.SetText`。事件通道保留给非 UI 调用方 |
| `ClientEvent.ShowConfirm`(10302)、`UIManager.ShowConfirm` | 面板（`ConfirmUnit`）已建好，**既无人触发也无人订阅** → 确认对话框没接线 |
| `ClientEvent.ShowFloatingText`(10303) | 定义了，无人触发也无人订阅（飘字实际走 BattlePage 内部方法） |
| `ClientEvent.OnRightClickBlocked`(10304) | BattlePage 订阅了，**无人触发** → 该提示永远不会出现 |
| `ClientEvent.OnEntityDisplayRemove`(10231) | 有 2 处触发，**无人订阅** |
| `TransitionManager` | 只在 `Tool` 里有静态字段，无任何转场逻辑 |
| `SaveManager.UnlockCharacter` | 无写入方（代码注释已标注；解锁实际走角色 SO 的 `unlockPlayerLevel` 自动判定） |
| `RandomMapper`、`ModSpace` | 零引用（未使用的工具类） |

### 2.3 数值待定稿（不是逻辑缺失，但影响可玩性）
- Config 的 4 个 region 明确标注"占位初值，待策划定稿"：通用 Buff 数值 / 防守方技能参数 / 技能参数补充 / 防守方被动数值
- 79 个技能的倍率 / 判定半径 / 弹道时长同样为占位；`initial_skills` 进攻方 18 人分配为临时值
- 头顶血条的表现参数（宽高、偏移、颜色）为占位，集中在 `LabelPlayerManager` 顶部常量区

### 2.4 代码卫生（可顺手清理，不影响功能）
- 注释与实现不符：`BattleManager.cs` 的 `#region 战斗规则（TODO：完整实现）` 标题应删掉 TODO（region 内 `StartBattle`/`EndBattle` 已完整：胜负判定与结算都对上策划案 17.2/17.3）、`BattleManager.cs:671` 注释描述未实现的 AI 行为、`MotionBase.cs:8` 说速度在 TODO 中消费（实际已消费）、`AssetsManager.cs:66` 与 `InfoManager.cs:70` 写"TODO：各分类模板配置后生效"（实际已配置）、`EntitySkillController.cs:8` 写"技能包实现待完善"（实际 79 个技能类已全部实现并注册）
- ~~`EntityData.OnKilled` 的 `// TODO: 掉落/击杀事件/愈战愈勇等`~~ → **已删（2026-09-23）**：这三件事都已在 BattleManager（掉落 = `TryDropCrystalWeapon`、复活叠层 = `RespawnPlayer`、击杀计数 = `KillCountByClient`/`DefenseKills`）
- `ClientEvent.cs:29` 记录的"10214~10219 已废弃"注释可清理
- `Vector3Serializer` 注释写"x(double 8)"共 24 字节，实际用 FloatSerializer 各 4 字节共 12 字节
- `EntityAnim.CharcterAnimType` 拼写错误（应为 `CharacterAnimType`），改名会影响资产，建议保留
- `SCBattleEvent.Type` 内嵌类名易与 `System.Type` 混淆
- 重复资产：`Assets/Files/Anim/CharacterAnim 1.controller`（30 个状态、无行为脚本、**无人引用**）可删

### 2.5 已完成、无需处理
- 技能池 **79 个技能类全部实现**（水晶武器 0~49 / 防守方 18 个，空位是被动 / 非玩家 8 个 / 空手 180~182），`SkillManager.RegisterAll` 全量注册
- **技能的延时能力已具备**：延时 / 多段 / 重复结算统一用 `Timer.AddTimer`（见《代码架构说明》Utils 节与技能节），不需要另做"延时模块"
- Buff 系统：双轨属性 + 查询式战斗管线（护盾→减伤→受伤乘区→扣血→反伤）、强控打断位移与置零动画速度、破霸体、DoT/光环 tick
- 子弹容器：时间戳推进、穿透（命中不消失、同一发对同一目标只结算一次）、上一帧→当前帧胶囊覆盖路径防穿模；`Damageable` 的伤害窗口机制已接线
- 战斗规则：世界生成、水晶定时重生、夜间僵尸刷新、权威移动、死亡复活与愈战愈勇、守护点分层减伤、分数制胜负、对局结算面板、视野双层可见性 + 阵营共享小地图、防守方三类被动
- 僵尸寻路（2026-09-23）：`ZombieEntityData.MoveTo/Stop`，能力已就绪、等 AI 决策层调用
- 每人击杀数（2026-09-23）：`BattleManagerCombat.KillCountByClient`（仅敌方玩家角色，暂不下发）
- 加载界面（2026-09-23）：连接服务器期间显示加载遮罩，见 2.2 的 `ShowLoading` 行
- 客户端逻辑层：`ClientLogicManager` 总控 + 5 个子管理器（2026-09-14 新增）
- 存档：双等级制数据、升级经验公式、角色解锁
- **按策划案 [A] 明确不做**：匹配功能、PVP/PVE 队列、自动 AI 补位、自动阵营分配（房间内玩家自由编辑双方 AI 数量、手动选阵营）

---

## 三、架构规范提醒（写后续代码时遵守）
- 服务器↔客户端传输的类：`SC` 开头（服务器→客户端）/ `CS` 开头（客户端→服务器），序列化器写在同一文件内
- ScriptableObject 一律 `Info` 结尾
- 一个类内不影响理解的计算函数放最后，用 `#region//Local` 包裹
- 全局参数被动（复活速度、僵尸刷新等级等）开战一次性计算，不做成 Buff
- 客户端只做表现，关键逻辑全部服务器计算
- **客户端逻辑一律进 `ClientLogicManager` 的子管理器**（普通 C# 类，继承 `ClientSubManager`）：能并入既有子管理器就别新建；`NetworkManager` 只负责把摘要转给对应子管理器，UI 的数据来源仍是 `ClientEvent` 事件
- 事件订阅与解绑必须成对（`EventManager` 是静态的，只订阅不解绑会随场景重载累积）
- 服务器与客户端都需要的逻辑（如轨迹构建）写成双端共用的函数，不要各写一份
- **延时 / 重复调用一律用 `Timer.AddTimer`**（`Utils/MonoUtils/Timer.cs`，由 `Tool` 每帧驱动），禁止自己写计时字段；技能的多段与延时结算同样走它，不要另建"延时模块"
- 注释只解释"为什么"，不复述代码、不写变更历史，禁止大段注释
