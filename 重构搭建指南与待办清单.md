# V0.8 框架重建后 · 用户待办清单

> 面向 V0.8 框架的搭建与配置说明
> 本清单列出需要你在 Unity 编辑器中完成的事（代码无法自动完成的部分），以及当前框架中留空待你后续实现的模块。

---

## 一、需要你在 Unity 里做的事（按顺序）

### 1. 打开项目并生成网络代码（必须第一步）
- [ ] 用 Unity 2022.3.62 打开项目 `RevelryOfSepulcher`
- [ ] 菜单栏 **Ens - Generate Code**，重新生成 `Assets/Scripts/EnsNetcode/Gen/NetworkManager.Generated.cs`
  - 未生成则 RPC 无法工作
- [ ] 观察 Console 报错，把报错信息反馈给我，我来修复

### 2. 服务器场景（DedicateServer.unity）
在场景中创建 `ServerRoot` 空物体，挂载：
- [ ] `RSServerLauncher`（已有脚本）
- [ ] `Tool`（全局工具/管理器引用）
- [ ] `NetworkManager`
- [ ] `BattleManager`
- [ ] `InfoManager`（服务器模板与属性配置）
- [ ] `EnvironmentManager`（昼夜）
- [ ] `EnsCorrespondent`、`EnsSpawner`（EnsNetcode 插件组件）
- [ ] 地形点位来源 `LandscapeSpawns`——**服务器也需要地图点位才能生成对局世界**（守护点/水晶/塔/树/僵尸/双方出生点均在服务器计算后下发）
  - 已定：**服务器与客户端场景各挂一份 `LandscapeSpawns`**，使用同一套世界坐标点位；**服务器侧只提供点位，不含贴图与图形表现**（服务器不打包素材）
- 说明：架构规定 **Server 场景不包含 AssetsManager**（不打包素材），服务器场景也不需要 InputManager（输入仅客户端使用，不再自动挂载）

### 3. 客户端场景（MainScene.unity）
创建 `ClientRoot` 空物体，挂载：
- [ ] `Tool`、`NetworkManager`、`BattleManager`、`InfoManager`、`SaveManager`、`EnvironmentManager`、`CameraController`、`ClientLogicManager`、`ClientDisplayManager`、`InputManager`（需手动挂载，不再自动添加）
- [ ] `AssetsManager`（客户端才有）
- [ ] `UIManager`（UI Toolkit）+ 场景中创建 `UIDocument`（PanelSettings 需在 Project 面板创建 UI Toolkit Panel Settings 资产，并拖给 UIDocument），再把 UIDocument 拖给 UIManager.uiDocument
- [ ] `EnsCorrespondent`、`EnsSpawner`
- [ ] 客户端场景挂 `Tool` 时注意：`Tool.Update` 里有 `Thread.Sleep(20)`，如果觉得卡可把 Inspector 上的 `delay` 改为 0

### 4. 配置 InfoManager（服务器与客户端各一份）
在 Project 面板创建配置资产并拖入对应列表（**地图点位不在这里**，见第 5 步）：
- [ ] 角色属性：`Assets/Files/Info/` 下创建 `EntityAttributeInfo`（Create → Ros/EntityAttributeInfo）
  - 进攻方 18 个 → `AttackCharacterInfoList`
  - 防守方 6 个 → `DefenseCharacterInfoList`（PC104 鹿铠 / NP114 白眼伯爵 / PC106 死灵漫步者 / NP134 蒙面教皇 / PC102 瘟疫使者 / PC103 苍白舞者，可先用同一资产占位）
  - 僵尸/精英僵尸/守护点/水晶/防御塔/瘟疫树/蘑菇各 1 个 → 对应字段
- [ ] 技能配置：创建 `SkillInfo`（Create → Ros/SkillInfo），填入技能 id/名称/CD，拖入 `SkillInfoList`（技能包实现后生效）
- [ ] 服务器模板：`AttackCharacterTemplates` 等（只含组件无图形的预制体，见架构说明）

### 5. 配置地形点位（LandscapeSpawns，全项目唯一点位来源）
点位统一配置在地形预制体 `Assets/Files/Prefabs/Landscape/Level.prefab` 的 `LandscapeSpawns` 组件上；`InfoManager` 不再持有任何点位字段。
**存储形态（共 7 组点位）**：① **锚点（Transform）** 5 组——守护点、防御塔、瘟疫树、进攻方与防守方的「出生/复活位置」：在场景里建空物体摆好位置，把对象拖进列表槽位即可，之后拖动对象就改了点位（运行时读 `Transform.position`，所以锚点必须是地形预制体的一部分，服务器那份才拿得到）；② **坐标（Vector3）** 2 组——水晶与僵尸出生点：由右键菜单批量生成。列表允许留空位或整体为空（为空时回退地图中心 `Landscape.MapCenter`）。
- [ ] 守护点 `beaconSpawnPositions`（Transform）：**3 外围 + 1 中心**（顺序固定——前 3 个为外围，第 4 个及以后为中心）
- [ ] 水晶 `crystalSpawnPositions`（Vector3）：类型按顺序循环 4 类
  - 右键菜单 **「随机生成水晶刷新点（清空后重建）」**：按 Terrain 表面高度打点，**越靠 Terrain 中心密度越高**，每点保证周围 0.5m 无其它 collider 且不与已配置点位重叠；数量由 `crystalGenerateCount` 控制（默认 100），**生成前会清空该列表**
- [ ] 防御塔 `towerSpawnPositions`（Transform，被摧毁后不复活）
- [ ] 瘟疫树 `plagueTreeSpawnPositions`（Transform，多个候选**随机取一个**）
- [ ] 僵尸出生点 `zombieSpawnPositions`（Vector3）
  - 右键菜单 **「随机生成僵尸出生点（清空后重建）」**：与水晶同一套流程，但**越靠 Terrain 边界密度越高**（到最近边的距离归一化）；数量由 `zombieGenerateCount` 控制（默认 100），**生成前会清空该列表**
- [ ] 进攻方 `attackPositions`、防守方 `defensePositions`（Transform）：**出生与复活共用同一个列表**——一方配 n 个位置，开局出生和死亡复活都从中**随机取一个**（不做去重，可能出现两个单位落在同一点）
- 说明：以上两个菜单都要求场景中有带 TerrainData 的 Terrain（脚本用宽度优先搜索自动查找）
- 说明：`LandscapeSpawns` 未随场景加载时，取用 `Tool.LandscapeSpawns` 会在 Console 报错（设计上是致命前提，代码不做事后兜底）
- 说明：组件已用 Gizmos 常驻显示全部点位（颜色按列表区分，每个点带一条向上立柱，立柱长度 = 该类型半径 × 2）。**Gizmos 半径分两个字段，可在 Inspector 调**：`crystalGizmoRadius`（水晶）与 `otherGizmoRadius`（除水晶外的全部点位，含僵尸与所有锚点），默认均为 1；地图 1280 单位，缩放到能看全地图时半径 1 直径约 1.6 像素，看不清就把这两个值调大

### 6. 配置 AssetsManager（仅客户端）
- [ ] `AttackCharacterGraphics` / `DefenseCharacterGraphics`（角色模型/预制体）
- [ ] 特效列表：`BulletVFX`(20) / `ShieldVFX`(13) / `RangeMagicVFX`(10) / `MagicCircleVFX`(10) / `BuffVFX`(31)（编号见策划案 2.4）
- [ ] 武器预制体：可用菜单 **Tools/武器/将选中场景物体创建为武器预制体** 生成，再拖入 `MeleeWeaponPrefabs` / `GunWeaponPrefabs` / `MagicOrbPrefabs`

### 7. 联调验证
- [ ] 先开服务器场景 → Play（RSServerLauncher 会自动 StartHost，日志出现 "Server Started"）
- [ ] 再开客户端场景（编辑器里可另开一个编辑器实例）→ Play → 初始界面 → 分别选进攻/防守角色 → 填服务器 IP（本机留空）→ 连接服务器 → 进入组队大厅 → 选队伍、编辑双方 AI 数量 → 开始游戏
- [ ] 预期：连接成功 → 组队大厅（房间状态广播）→ 开始游戏 → 进入战斗页 → 底部技能栏、顶部时间/昼夜/分数、右侧守护点血量出现
- [ ] 把任何编译错误/运行报错贴给我

---

## 二、框架已搭好、逻辑留空待你/后续实现（代码中均标 TODO）

| 模块 | 现状 | 待实现 |
| --- | --- | --- |
| **技能包** `Skills/SkillPackageA/B/C.cs` | 包结构 + 注册表已就绪 | 具体技能：武器（近战三连/剑气/弹幕/重型）与角色技能、防守方 6 角色主动/被动/大招（见策划案 4.2/11） |
| **Buff 系统** `EntityEffectController` | 类型枚举（策划案 9.2 三类）+ 增删查 API 已就绪 | 时长推进的 DoT、属性影响计算（移速/减伤/视野）、特效绑定（特效编号见 9.2） |
| **子弹容器** `BattleManager.ShootBullet` | 接口已留（Bullet/BulletPlayer/Damageable 保留） | BulletContainer 位置推进、命中检测、伤害结算 |
| **战斗规则** `BattleManager.ManagedUpdate` | 计时/昼夜推进/实体更新/死亡列表/同步框架已跑 | 服务器权威移动与近战/滑铲计算、守护点减伤分层、分数制胜负、僵尸 AI、水晶/塔/树交互、复活进度与愈战愈勇 |
| **匹配** | Lobby 直接连接进场 | PVP/PVE 队列、AI 填充、防守方分配规则（近期担任次数） |
| **环境表现** `EnvironmentManager.ApplyVisual` | 阶段状态机已跑 | 天空盒/灯光/雾的视觉表现 |
| **客户端表现细节** `ClientDisplayManager` | 创建/更新/移除表现物体已跑 | 血条/头顶信息、技能特效调用、死亡表现细化 |
| **存档数值** `SaveManager` | 双等级制数据结构已就绪 | 升级经验表、玩家等级解锁表（策划案 17.11/36） |
| **UI 细化** | 结构完整 | 图标资源绑定、右键阻断提示文案、滑铲表现等 |

## 三、架构规范提醒（写后续代码时遵守）
- 服务器↔客户端传输的类：`SC` 开头（服务器→客户端）/ `CS` 开头（客户端→服务器），序列化器写在同一文件内
- ScriptableObject 一律 `Info` 结尾
- 一个类内不影响理解的计算函数放最后并用 `#region//Local` 包裹
- 全局参数被动（复活速度、僵尸刷新等级等）开战一次性计算，不做成 Buff
- 客户端只做表现，关键逻辑全部服务器计算
