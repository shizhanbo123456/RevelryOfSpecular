# RevelryOfSepulcher 代码架构速查

更新时间：2026-08-09  
分析范围：`Assets/Scripts` 当前代码（已同步 8 月以来技能/输入/相机/UI/伤害公式等全部改造）

## 0. 目录与文件速查

```
Assets/Scripts/
├── Client/                客户端辅助（partial 扩展、事件定义、奖励 DTO）
│   ├── ClientEvent.cs         事件 ID 常量（ClientEvent）+ BattleKillSummary/BattleHealthInfo/BattleStaminaInfo/BattleSkillRuntimeInfo
│   ├── LevelLogic.cs          服务端关卡逻辑（partial BattleManager）：区块感染、资源刷新、伤害公式 FigureDamage、子弹命中 BulletHit
│   └── RewardInfo.cs          SettleRewardInfo / RuntimeRewardInfo（结算奖励结构）
├── Editor/                编辑器工具
│   └── SkillDebugger.cs       技能调试面板（Tool/SkillDebugger）：发射源/发射目标双 Transform、弹道/判定球/扫掠胶囊可视化
├── Entity/                实体体系
│   ├── Attribute/
│   │   ├── EntityAttribute.cs       属性类（health/attack/defense...，Clone/GetValueList）
│   │   └── AttributeInfo.cs         EntityAttributeInfo ScriptableObject + GetAttribute(level) 成长计算
│   ├── Bullet/
│   │   ├── Bullet.cs                子弹结构（rate/curve/radius/lifeTime/damageable/attribute 快照，Position/LastPosition/InDamageWindow）
│   │   ├── BulletPlayer.cs          客户端特效播放器（沿贝塞尔曲线）
│   │   └── Damageable.cs            命中策略：Once（每目标一次）/ CD(cd)（周期伤害）/ AvailableLifeTime(窗口)
│   ├── Client/
│   │   └── EntityPlayer.cs          客户端实体显示组件（动画刷新）
│   ├── EntityComponents/
│   │   ├── EntityData.cs            实体基类（id/type/属性/碰撞胶囊/效果控制器，OnCreate/OnUpdate/OnDamaged）
│   │   ├── PlayerData.cs            玩家实体（Rigidbody 移动）
│   │   ├── NpcData.cs               NPC 实体（NavMeshAgent）
│   │   ├── PropData.cs              资源实体（植物/矿石）
│   │   └── InfectionData.cs         大型感染体实体
│   ├── Identify/
│   │   └── EntityType.cs            EntityCategory/EntityType + EntityTypeExt 扩展（IsCharacter/IsNpc/IsPlant/IsOre/IsInfection...）
│   ├── Landscape/
│   │   ├── ChunkSearcher.cs         区块空间索引（Add/Remove/范围查询/RefreshAll）
│   │   └── Landscape.cs             场景地图配置（出生点/撤离点/锚点/感染 mask）
│   └── Skills/                      技能三包 + 基类
│       ├── SkillBase.cs             抽象基类：DelayActs（经 Tool.SkillDelayActionManager）、FR 伪随机、CircleDests/FanDests/RotateDest/JitterCircleDests、ShootC/PlayC 系列
│       ├── SkillInfo.cs             技能信息（品质/感染标识/图标）
│       ├── SkillManager.cs          技能总入口（DoDamageActs/PlayVFX/GetSkillCD/GetSkillQuality/GetSkillWeight）
│       ├── SkillPackageA.cs         玩家技能 0-30（每个技能 DoDamageActs/PlayVFX 成对，特效与伤害范围统一）
│       ├── SkillPackageB.cs         植物/矿石烟雾 1000-1018（BuffMap 表按 id 附加 buff）
│       └── SkillPackageC.cs         投射物 2000-2999（ConfigMap 查表驱动：颜色/样式/roll/拱高/距离）
├── GameController/        全局管理器（服务定位）
│   ├── Tool.cs                     全局服务定位器（静态引用所有 Manager + ActiveFor/ExpandListTo 工具）
│   ├── EventManager.cs             事件中心（无参/单参泛型事件总线）
│   ├── SkillDelayActionManager.cs  技能延时管理器（Tool 单例，持有 SkillBase 两个 DelayActs 参数版本，驱动 GenericTimer）
│   ├── TransitionManager.cs        过渡管理器（Execute(Func<float,bool>,time,onFinish)/Cancel）
│   ├── BattleManager.cs            服务端权威（partial）：EntityContainer/BulletContainer/OutputBuffer、ManagedUpdate 主循环、OnDrawGizmos 子弹调试绘制
│   ├── InfoManager.cs              配置入口（EntityAttributeInfo/SkillInfoList/服务端模板）
│   ├── AssetsManager.cs            客户端资源入口（SkillVFX/Dusts/Projections/实体 prefab/图标）
│   ├── NetworkManager.cs           网络层（客户端连接/同步 RPC 转事件、服务端接收转发）
│   ├── InputManager.cs             统一输入访问（MoveInput/ToggleCameraPressed/MouseDelta/LeftSkillPressed/RightSkillPressed/SkillScroll + TryGetCenterSkillPosition）
│   ├── CameraController.cs         相机（第三人称轨道 / 俯视 (0,10,-4)，Yaw 按模式返回）
│   ├── ClientLogicManager.cs       客户端世界逻辑入口（6 个子管理器 + LabelPlayer_ 飘字配置 + TryGetNearestEnemy 转发）
│   ├── SaveManager.cs              存档（PlayerPrefs+JSON，角色/印记/符文/碎片）
│   ├── EnvironmentManager.cs       环境管理器
│   ├── GizmosDrawer.cs             编辑器绘制工具（带时间渐变任务队列）
│   ├── GizmosDrawerUpdater.cs      GizmosDrawer 的绘制载体（顶层 public 类，AddComponent 可用）
│   ├── Loading.cs                  加载页
│   └── GameController/客户端辅助     （见 UI 层）
├── Server/                 固定单房间模型
│   ├── RSServerLauncher.cs         服务端启动器
│   ├── RSRoomManager.cs            房间管理器（默认固定房间）
│   └── RSRoom.cs                   房间（分配 authority/接收加入）
├── Transport/              跨端 DTO + 序列化
│   ├── CSInputCommand.cs           移动命令（moving/yaw）
│   ├── CSPlayerInfo.cs             客户端入场配置
│   ├── SCPlayerInfo.cs             服务端回执（玩家实体 ID/出生点）
│   ├── SCEntityDisplayInfo.cs      实体显示同步（id/type/位置/yaw/血量/特效图形）
│   ├── SCOutput.cs                 输出（飘字/技能表现）
│   ├── InteractablePropInfo.cs     交互物状态（解锁道具/撤离点 bitflag）
│   ├── LevelInfo.cs                关卡摘要
│   ├── NetworkEvent.cs             击杀统计事件表
│   └── SCPvpKillRewardInfo.cs      PVP 击杀奖励
├── UI/                    页面层
│   ├── PageBase.cs                 页面基类（Init/Enter/Exit）
│   ├── UIManager.cs                页面管理（PageType 枚举）
│   ├── HomePage.cs                 主页五页签（角色/印记/收集/技能/探索 + 登录区）
│   ├── BattlePage.cs               战斗页（批量渲染飘字/血条：ActiveFor + OnUpdateSceneLabels/OnUpdateSceneEntityBars）
│   ├── AwardPage.cs                结算页
│   ├── Notice.cs                   提示（中央弹窗 ShowNotice / 侧边 ShowSideNotice）
│   ├── VfxPlayer.cs                特效播放
│   └── Units/
│       ├── BattleLabel.cs          飘字（AnimationCurve 驱动 alpha/scale/上飘）
│       ├── EntityBar.cs            血条
│       ├── BattleSkillUnit.cs      战斗技能槽（冷却/次数/选中高亮）
│       └── Home*Unit.cs            主页各页签单元（角色/印记/收集/技能/属性）
└── Utils/                 通用工具
    ├── BezierCurve.cs              贝塞尔曲线（GetLine/GetPoint/GetProjection）
    ├── GenericTimer.cs             泛型计时器（AddTimer<T>(value,delay,act) + Update 每帧驱动，由 SkillDelayActionManager 驱动）
    ├── ModSpace.cs                 多维参数编码/解码（旧 C 包用，现保留）
    ├── MultiObjectSource.cs        多 prefab 源集合
    └── RandomMapper.cs             稳定随机映射
```

> 注：`SkillDelayActionManager`、`GizmosDrawer`、`GizmosDrawerUpdater` 等均注册到 `Tool` 静态引用（Awake 赋值）。

## 1. 总体架构

**服务端权威战斗 + 客户端显示/输入 + EnsNetcode 房间通信**。

- 服务端核心：`BattleManager`（+ `Client/LevelLogic.cs` partial 扩展）、`RSRoomManager`、`RSRoom`。
- 客户端核心：`NetworkManager`、`ClientLogicManager`、`UIManager`、`InputManager`、`CameraController`。
- 全局基础设施：`Tool`（服务定位）、`EventManager`（事件中心）、`GenericTimer`/`SkillDelayActionManager`（延时）、`TransitionManager`（过渡）。
- 实体：`EntityData` 派生（`PlayerData/NpcData/PropData/InfectionData`）+ `EntityPool` 实例化/回收 + `ChunkSearcher<T>` 区块索引。
- 技能：`SkillManager` 总入口 → `SkillPackageA/B/C` + `SkillBase` 工具族；子弹 `Bullet`/`Damageable`/`BulletPlayer`。
- 网络插件：`EnsNetcode`（`EnsBehaviour`、`[Rpc]`、`CallFuncRpc`、`Delivery` 可靠性），业务代码通过 partial 类接入。

## 2. 服务器 / 客户端分工

| 层面 | 服务端（Windows Server / Host） | 客户端 |
|---|---|---|
| 实体 | `EntityContainer.SpawnEntity` 生成权威实体，`OnUpdate` 推进 | 只接收 `SCEntityDisplayInfo` 同步，`EntityPlayerManager` 生成显示 prefab 插值 |
| 伤害 | `BulletHitCheck` + `FigureDamage` 计算命中与伤害（权威） | 只播放 VFX/飘字（`SCOutput`/事件） |
| 子弹 | `BulletContainer` 管理、`RemoveExpiredBullets`、`AvailableLifeTime` 窗口 | `BulletPlayer` 沿曲线播放表现 |
| 输入 | `ReceiveCommand` → `PlayerData.MoveTo`；技能 RPC 收 `skillId/dest` | `InputManager` 采集 → `CSInputCommand`/`SendUseSkillRequest` |
| 世界刷新 | `ManagedUpdate` → `GameLogic`：感染值、植物/矿石/僵尸/感染体刷新、撤离点 | 无 |
| 结算 | `OutputBuffer.AddEvent`（击杀/掉落统计）→ RPC | `SettlementManager` 检测撤离区、`AwardPage` 展示 |
| 资源 | 不引用 `AssetsManager`（服务端抛异常） | `AssetsManager` 加载所有视觉资源 |

关键约定：**客户端不做权威伤害**；服务端不加载视觉资源。`BattleManager.ManagedUpdate` 开头 `if (Tool.AssetsManager != null) return;` 仅服务端执行。

## 3. 全局基础设施（重要类）

### Tool.cs —— 全局服务定位器
静态字段：`AssetsManager / InfoManager / TransitionManager / BattleManager / NetworkManager / InputManager / CameraController / SaveManager / UIManager / ClientLogicManager / EnvironmentManager / SkillDelayActionManager`。各 Manager 在 `Awake()` 写入 `Tool.Xxx`。工具方法：`ActiveFor<T>`（批量激活/实例化）、`ExpandListTo<T>`、`GetTemporaryTransform`。

### EventManager.cs —— 事件中心
- 无参：`AddEvent(int, Action)` / `TrigEvent(int)`
- 单参泛型：`AddEvent<T>(int, Action<T>)` / `TrigEvent<T>(int, T)`
- 事件 ID 来源：`ClientEvent`（客户端 UI/战斗显示，如 `ShowNotice=10000`、`UpdateSceneLabelsEvent=10308`、`UpdateSceneEntityBarsEvent=10309`）+ `NetworkEvent`（击杀统计）
- 限制：不支持多参数，复杂数据用 tuple 或 DTO 包装；同 ID 只能绑定一种泛型类型（有运行时校验）

### GenericTimer.cs —— 泛型计时器（Utils/）
```csharp
GenericTimer.AddTimer<T>(T value, float delay, Action<T> act);  // delay<=0 立即执行
GenericTimer.Update();                                          // 由 SkillDelayActionManager.Update 每帧驱动
```
闭包捕获 value 快照；复用 List，无每帧 GC。

### SkillDelayActionManager.cs —— 技能延时管理器
- Mono 单例，`Awake` 写 `Tool.SkillDelayActionManager`
- 持有 `SkillBase` 两个 `DelayActs` 参数版本：
  - `DelayActs(float, (EntityData,Vector3,Vector3), Action<...>)` 服务端
  - `DelayActs(float, (Vector3,Vector3), Action<...>)` 客户端表现侧
- 内部转调 `GenericTimer.AddTimer`；**场景需挂载该组件**，否则 DelayActs 空引用

### TransitionManager.cs
`Execute(Func<float,bool> action, float time, Action onFinish)` 返回 taskId，`Cancel(taskId)` 可中断。技能延时已迁移到 GenericTimer，此处仍供其他过渡使用。

## 4. 主要功能模块

### 4.1 输入（InputManager）
统一输入访问：每帧 `RefreshInputStates()` 缓存 → 公共只读属性：
`MoveInput`（WASD）、`ToggleCameraPressed`（切换相机键，Inspector 可配 `toggleCameraKey`）、`MouseDelta`（滑动即转视角）、`LeftSkillPressed`（左键）、`RightSkillPressed`（右键）、`SkillScroll`（滚轮）。全项目仅此处直接访问 `Input`。`TryGetCenterSkillPosition()` 提供屏幕中央射线定位（技能目标）。

技能选择/使用逻辑在 `ClientSkillManager`：滚轮切换槽位、左/右键释放（读取 InputManager 状态）。

### 4.2 相机（CameraController）
- `CameraMode { ThirdPerson, TopDown }`，默认第三人称；按 `ToggleCameraPressed` 切换
- 第三人称：鼠标滑动即转（pitch/yaw），轨道跟随
- 俯视：相对玩家固定 `(0,10,-4)` 看向玩家，不响应旋转
- `Yaw` 按模式返回：第三人称=轨道 yaw；俯视=相机实际朝向（保证 WASD 相对相机移动正确）
- 俯视→第三人称切换时 `yaw = 玩家朝向`，相机落在角色后方

### 4.3 技能释放目标（ClientSkillManager）
| 模式 | 左键 | 右键 |
|---|---|---|
| 第三人称 | 屏幕中央（`TryGetCenterSkillPosition`） | 最近敌人（10m），无则屏幕中央 |
| 俯视 | 最近敌人（10m） | 最近敌人（10m），无则 `ShowNotice`"周围无有效攻击目标" |

`EntityPlayerManager.TryGetNearestEnemy(center, radius, out pos)`：排除本地玩家与纯资源型实体（植物/矿石）。

### 4.4 技能系统（Skills/）
- **SkillManager**：`DoDamageActs(id, entity[, dest])` 服务端执行、`PlayVFX(id, pos, dest)` 客户端播放；按 0-30 → 1000-1018 → 2000-2999 依次尝试三个包
- **SkillBase**：`DelayActs`（延时）、`FR(i)` 伪随机固定序列（服务端/客户端结果一致）、`CircleDests/FanDests/RotateDest/JitterCircleDests`（终点计算）、`ShootC/PlayC/ShootCFrom/PlayCFrom/DelayShootC/DelayPlayC`（C 包子弹成对调用）
- **PackageA（0-30）**：31 个技能类，`DoDamageActs`/`PlayVFX` 成对、同源 dests —— **特效与伤害范围统一**（曾修 Skill27/29 垂直错位、Skill25/26 飞行时长脱节）
- **PackageB（1000-1018）**：植物/矿石烟雾；`BuffMap[19]` 按 id-1000 附加 buff（深红生命流失/品红狂化/浅蓝冰冻/深绿中毒...），即时类 time=0
- **PackageC（2000-2999）**：投射物；`ConfigMap` 查表（color/style/roll/projectionRadius/distance），id 与 `Projections[color+style*5]` 对齐；`TryShootFrom/TryPlayFrom` 支持自定义发射源
- **子弹**：`Bullet` 携带 `attribute`（攻击者 floatingAttribute 引用快照）；`Damageable.Once/CD/AvailableLifeTime`；`BezierCurve.GetLine/GetPoint/GetProjection`

### 4.5 伤害公式（Client/LevelLogic.cs FigureDamage）
```csharp
damage = (float)attacker.attack / Mathf.Max(100, defense + attack) * attack;  // 已修 int 整数除法 bug
damage *= rate;  // 技能倍率
// 暴击：strikeRate - strikeRateResistance > rand(0,100) 时 ×(strikeDamage-strikeDamageResistance)/100
```
命中判定：子弹本帧线段 (LastPosition→Position) 与目标碰撞胶囊的最近距离 < `b.radius + 目标半径`。

### 4.6 客户端世界显示（ClientLogicManager 子管理器）
| 子管理器 | 职责 |
|---|---|
| `EntityPlayerManager` | 实体显示同步/插值、`TryGetNearestEnemy` |
| `LabelPlayerManager` | 飘字/血条**数据打包 → 事件推送**（`SceneLabelData`/`SceneEntityBarData`）；`TryWorldToParentPoint`（public static） |
| `ClientInteractManager` | 交互物/撤离点刷新与拾取检测 |
| `SettlementManager` | 撤离进度/击杀统计/奖励结算 |
| `ClientStaminaManager` | 体力 |
| `ClientSkillManager` | 选中槽位/滚轮切换/技能释放/CD/剩余次数 |
| `ClientBattleTimeManager` | 战斗计时 |

飘字/血条事件化：`LabelPlayerManager` 每帧打包 `List<SceneLabelData>`（含 3 条 AnimationCurve）触发 `UpdateSceneLabelsEvent(10308)`，`BattlePage.OnUpdateSceneLabels` 用 `Tool.ActiveFor` 批量激活/刷新；血条同理（10309）。动画参数来自 `ClientLogicManager` Inspector 的 `LabelPlayer_*` 字段（AlphaCurve/ExistTime/ScaleCurve/RiseCurve）。

### 4.7 地图与空间索引

**`Landscape.cs`（Assets/Scripts/Entity/Landscape/，单例 `Landscape.instance`，场景地图配置）**：

- **地图参数**：`chunkCount=15`、`chunkSize=20`，地图总尺寸 = 15×20 = 300m；`WorldToChunkIndex`（世界坐标→区块索引）、`WorldToUV`（世界→贴图 UV 0~1）、`GetChunkFourCornerUV`（区块四角 UV）。
- **Transform 点位列表（Inspector 配置）**：
  - `SpawnPosList`：玩家出生点（随机取一个）。
  - **`ExitPortList`：唯一的撤离点锚点列表**（位置固定，等级不预置；由服务器按所在区块感染值以线性概率动态决定开启等级，见撤离系统）。
  - `CharacterUnlockPropPos`：角色解锁道具点位。
- **随机生成锚点（四类）**：`PlantSpawnAnchors / OreSpawnAnchors / InfectionSpawnAnchors / ZombieSpawnAnchors`（Inspector 可调数量 `plantSpawnCount` 等），`GenerateAllSpawnAnchors`/`ClearAllAnchors` 为 ContextMenu 编辑器操作。
- **地形掩码贴图 `maskTexture`**：R 通道=区块禁区（区块四角任一 R>0.5 整块禁用）；B 通道=点位精细过滤（采样点 B>0.5 该坐标不能生成锚点）。
- **区块双缓存**：`_invalidChunkCache`（禁用）/ `_validChunkCache`（有效），`RefreshChunkCache()` 一次性重建，供感染刷新/撤离判定快速查询。
- **Gizmos 可视化**：三类 Transform 点位（出生/撤离/解锁，`drawTransformListGizmos`）、四类生成锚点（`drawSpawnAnchorGizmos`）、无效区块线框。

**`ChunkSearcher<T>`**：通用区块索引容器（Add/Remove/范围查询/RefreshAll），地图外对象归 (-1,-1)。

### 4.8 配置中心（Config.cs，Assets/Scripts/根目录）

**约定：所有可调数值、概率、区间、上限、文案统一写入 `Config`，业务代码禁止硬编码数值**（新增数值先找 Config，找不到就加进去）。

`Config` 分区概览：
- **玩家带入属性**：技能槽 6、印记携带 3、入场负重上限 100、C 符文保底 999、技能键位列表。
- **操作配置**：WASD 键、鼠标按钮（左键=技能、右键=技能）、滚轮方向、相机参数（距离/高度/旋转速度/pitch 范围）、体力衰减 0.5、索敌半径 10m。
- **UI 与结算**：撤离读条 5/10/15s、撤离区半径 5m、掉落概率（僵尸 B5%/A2%/S0.3%、植物果实 20%、笔记 25% 等）、击杀经验（僵尸+1/感染+30/玩家+5）、飘字 UI 缩放。
- **通用配置**：`UpgradeConfig{level, exp}` 结构（**exp=升级到下一级所需经验**）、角色升级曲线（1~10 级）、印记升级曲线（每级 1/2/2/5/5/5/5/5/10/10）、实体数量（角色14/僵尸61/植物15/矿石8/感染7/印记30/技能31）。
- **关卡信息**：感染收敛（每 10s <100 +2 / >100 -1.5，`infection_converge_up/down_delta`，范围 10~300）、僵尸对感染影响（存活+0.1/s/刷新+1/死亡-4）、僵尸迁徙 CD 20~40s、重生 CD 90s、感染阈值（120~150 异变 / >180 刷新感染体）、**撤离点开启区间**（初级 [50,100] / 中级 [120,180] / 高级 [200,300]，appear=必开端、disappear=必不开端）、**撤离带出负重上限**（60/120/200）、**角色经验倍率**（1/1.5/2/0.3）、**基础印记产出**（1×3/2×5/3×6/5×8）、**撤离信物**（中级 30% 触发 0~2、高级 2~4 权重）。
- **其它配置**：14 角色职业/背景/被动文案、30 印记名称与效果文案、31 技能名。

### 4.9 网络与 DTO
- `EnsNetcode`：`EnsBehaviour`（partial）+ `[Rpc]` + `CallFuncRpc` + `Delivery.*`；改 RPC 后运行 `Ens - Generate Code`
- `Transport/`：CS*（客户端→服务端）、SC*（服务端→客户端）、`NetworkEvent`（击杀统计）、`InteractablePropInfo`（交互物 bitflag）
- `NetworkManager`：客户端连接/入场/输入上报/事件转换；服务端接收玩家信息、同步实体/飘字/技能

## 5. 关键数据流

### 入场
`HomePage` 选角/印记/技能 → `NetworkManager.EnterWorld()` → `ClientSendInfo()`（CSPlayerInfo）→ RPC `ServerReceivePlayerInfo` → `BattleManager.AddPlayerInfo()` 生成玩家实体 → RPC 回 `SCPlayerInfo`

### 输入与战斗（客户端→服务端→客户端）
```
InputManager 采集（WASD/滚轮/左右键）
→ ClientSkillManager 决定技能目标（中央/最近敌人）
→ SendMoveCommand / SendUseSkillRequest
→ BattleManager.ReceiveCommand/ReceiveUseSkill → PlayerData.MoveTo / SkillManager.DoDamageActs
→ BulletContainer 生成子弹 → ManagedUpdate.BulletHitCheck → FigureDamage 伤害
→ OutputBuffer（飘字/VFX/事件）→ RPC
→ NetworkManager 转 ClientEvent → ClientLogicManager 更新显示/飘字/血条 → BattlePage 渲染
```

### 技能表现（客户端回放）
`SkillManager.PlayVFX(id, pos, dest)` → `PackageA.PlayVFX` 实例化 `SkillVFX[i]` + `BulletPlayer` 沿曲线；C 包 `Projections[color+style*5]`；延时用 `SkillDelayActionManager`（GenericTimer）

### 撤离结算
`SettlementManager` 检测撤离区 → `ExitMax/Normal/Min` 事件 → `Settle()` 算奖励 → 写 `SaveManager` → `AwardPage` 展示

## 6. 调试与可视化

- **BattleManager.OnDrawGizmos**：运行时绘制所有子弹——黄色折线=弹道、红球=判定半径、橙球=命中范围（子弹半径+0.4）；`InDamageWindow` 内亮色、窗外灰色；`BattleManager.DrawBulletDebug` 开关
- **SkillDebugger（Tool 菜单）**：输入 SkillId + 发射源/发射目标双 Transform → 使用技能；面板可开关弹道/判定范围/**扫掠判定胶囊**（绿色，精确复现 BulletHit 线段判定）；`GizmosDrawer.DrawWireCapsuleBetween` 任意方向胶囊
- **GizmosDrawer/GizmosDrawerUpdater**：编辑器绘制任务队列（带时间渐变，RELEASE 裁剪）；Updater 为独立顶层 public 类（嵌套类 AddComponent 会失败——曾修）
- **SkillDebugger 注意**：测试实体 `AddComponent<InfectionData>()` 不触发 `OnCreate`，`floatingAttribute` 为 null，命中计算会 NRE

## 7. 当前风险 / 注意点

1. **植物/矿石 attack=0**（设计）：它们发起的攻击（B/C 包）source damage 恒 0，buff 仍生效
2. **SkillDebugger 测试实体** floatingAttribute 未初始化（AddComponent 不走 OnCreate）→ FigureDamage NRE
3. `Tool.Update()` 每帧 `Thread.Sleep(delay)` 人为降帧，正式版确认是否保留
4. 历史遗留（未改）：Skill1 主特效归一化 1m 短叉 vs 伤害真实距离；Skill18/19 特效低 1m；Skill11 黑球无单发弹道
5. **撤离等级判定链路**：服务器端 `LevelLogic.RandomInteractableProp` 已改为按感染值线性概率开启（区间 [50,100]/[120,180]/[200,300]）；客户端 `SettlementManager.GetCurrentExitMode` 仍按服务端广播的三类端口（Max/Normal/Min flag）判定等级，属正常消费链路，无概率逻辑（概率只在服务端）
6. **撤离信物/负重裁剪/角色经验公式**已按策划案落地（`AddExitModeTokenReward`/`TrimCarriedRunesByWeight`/`AddExitModeReward`）；旧 `Config.max/min/normal/lost_exit_base_exp` 已无引用（保留未删）
7. `NpcData.agent` 未在 OnCreate 初始化（此前记录，需复核）；`CSPlayerInfo.imprints` 初始化字典重复 key（此前记录，需复核）
8. 所有可调数值统一写入 `Config`，业务代码不硬编码（见 4.8 配置中心）

## 8. 维护约定

- **所有可调数值/概率/区间/上限/文案统一写入 `Config.cs`，业务代码禁止硬编码**（新增数值先找 Config，找不到就加进去，按现有 #region 分区归类）
- 新增跨端数据：先定义 `Transport` DTO → 补序列化 → RPC 签名变更后运行 `Ens - Generate Code`
- 新增 Manager：对应 `Awake()` 写入 `Tool.Xxx` 单例（如 `SkillDelayActionManager` 已挂场景）
- 服务端不引用 `AssetsManager`；客户端不做权威伤害
- 技能特效与伤害必须成对：`DoDamageActs`（服务端）与 `PlayVFX`（客户端）用同一 dest 计算函数（CircleDests/FanDests/FR 序列）
- 事件化 UI：数据层（LabelPlayerManager）打包 `List<T>` 触发事件，UI 层（BattlePage）`Tool.ActiveFor` 批量渲染，避免逐帧逐对象操控
- 所有"随机"用 `FR(i)` 固定序列而非 `Random`，保证服务端/客户端表现一致
