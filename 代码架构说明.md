下文中，无特殊提及，均默认在Assets\Scripts目录下
下文中[F]表示总体固定的文件部分，因为涉及到难以单独在开发目录中理解的功能，如涉及到动画、其它框架。该部分非必要不要改动，如果要改动请提前像用户征求同意
[E]表示提供了该类的引导、例子，可按照原有代码类比拓展更多功能元素进来
[T]表示可继续完善功能功能
其余无标记内容，按照需求编写即可

总体原则
原则上客户端只是接收服务器的信息摘要并将其转化为表现展示给玩家的，核心移动/战斗内容都在服务器完成计算，不让客户端处理关键逻辑，非关键逻辑可放在客户端（减少服务器压力/简化服务器代码）
如果代码中涉及了需要在项目中制作预制体等ai不方便操作的相关内容，则应当向用户具体描述需求
Server场景不包含AssetsManager(不能打包进素材文件)，其余GameController下内容都包含，添加RSServerLauncher启动服务器
Client场景包含GameController下全部内容
服务器上的实体应该只调用一个模板（只包含各类组件但没有图形的预制体，InfoManager引用），客户端的实体才会包含具体贴图模型表现
服务器上的实体模板可直接复制和销毁，不用Pool
尽量不要硬编码事件id或固定消息，可在GlobalData创建各类静态类，包含const的各类事件id，或者消息id和消息内容的映射表，便于后续修改和维护之类的，别的新功能有类似需求也可以添加相应的文件

代码规范：
需要在服务器和客户端之间来回传输的内容，如果只是特定方向的传输的，例如只是服务器传输到客户端，类名（或结构体名）和文件名要加上SC开头，客户端向服务器则为CS
所有ScriptableObject类都要以Info结尾
一个类中，如果有一些函数是必要但影响可读性的，例如局部计算处理数据的函数，不被外部引用，只看函数名不看具体实现不影响理解代码的，统一放在类(或结构体)最后并用"#region//Local"包裹

【注释规范】禁止在代码中写大段注释，注释必须言语简短、简洁说明：
只解释"为什么"，不复述代码"做了什么"（代码本身已经表达）
不写变更历史、不写已废弃方案的对比、不写"曾经如何/现在改为如何"
长篇设计说明、机制原理、协议约定一律写进《代码架构说明》等文档，不占用代码
禁止把废弃代码以注释形式留在原地，直接删除

GameController文件夹：
各类管理器，全部跟随场景加载初始化，客户端全部都有，服务器没有AssetsManager
其中的Utils子文件夹，内容为需要依赖特定管理器的常用工具或者是对管理器的再次封装
其中的Sub子文件夹，放「客户端逻辑管理器」（ClientLogicManager）的子管理器

ClientLogicManager（客户端总控）：
服务器摘要进入客户端后统一由它分发给各子管理器。它本身只做四件事——Awake 建全部子管理器并逐个 Init、Update 统一 Tick、OnDestroy 统一 Dispose——不含具体业务
子管理器是**普通 C# 类**（继承 ClientSubManager，不挂场景、不进 Tool），用 Tool.ClientLogicManager.<属性> 取用，名字与类名刻意不同以避免与类型名冲突：
- EntityPlayers（EntityPlayerManager）：表现视图的创建/更新/移除、位姿与动画同步、手上与悬浮武器、持续 Buff 特效、蘑菇感染换模、迷雾表现、超时兜底移除。对外提供 TryGetView / TryGetEntityPosition / TryGetEntityTransform（子弹轨迹取实体位置走这里，服务器侧仍走实体容器）
- Labels（LabelPlayerManager）：玩家头顶名字与血条。标签挂在表现视图下（跟随与层级沿用视图），朝向由它每帧对齐相机；订阅 OnEntityDisplayUpdate 刷血量
- Settlement（SettlementManager）：终局判定与局外经验入账。**局外经验只在这一处写存档**，UI 只负责显示；同时广播 ClientEvent.OnBattleEnd
- SkillVfx（ClientSkillManager）：客户端技能表现的唯一入口（PlayVFX 由它调用，后续音效/预测也在此扩展）
- BattleTime（ClientBattleTimeManager）：昼夜快照转交 EnvironmentManager；本机推演对局剩余时间；缓存权威分数快照

客户端数据流：NetworkManager 收到 SC 消息后，**表现类调用转给对应子管理器**，其余（战斗事件/小地图/复活进度/房间状态）**仍走 EventManager 事件**，UI（BattlePage/LobbyPage/UIManager）照旧订阅 ClientEvent，不因这层介入而改动
新增客户端逻辑时：先判断能否并入既有子管理器，不能才新增（在 ClientLogicManager 里创建并 Init）；订阅与解绑必须成对——EventManager 是静态的，只订阅不解绑会随场景重载累积

[T]Utils文件夹：
一些不依赖其它系统的通用工具
其中的MonoUtils也会被直接实例化挂载在场景中(Server/Client)

Utils/MonoUtils/Timer.cs：
**全项目延时/重复调用的唯一入口**，由 Tool.Update 每帧驱动——Tool 在服务器与客户端场景都有，所以两端都能用。
- `Timer.AddTimer<T>(T value, Action<T> act, float invokeInterval, int invokeTime, bool invokeInstantly)`：延时 invokeInterval 后执行 act(value)，共执行 invokeTime 次；`invokeInstantly = true` 表示**下一帧就执行第一次**（不是同步立即执行），false 表示第一次也要等满 invokeInterval。`invokeTime <= 0` 或 `act == null` 直接忽略
- 回调异常由 Timer 内部捕获并记日志，**不会中断同帧其它定时器**，也不会卡住自身（次数照常递减）
- **没有取消句柄**：注册后只能跑到次数用尽；需要"实体销毁就作废"的场合由调用方自行判空兜（如闭包里判 `entity != null`）
- 触发时刻按帧累减，实际间隔比 invokeInterval **略早（每周期约少 1 帧）**，不要拿它当精确计时基准（需要精确计时的地方用绝对时间戳比较）

[F]Server文件夹:
使用的网络插件的特定写法，单个服务器持有单个场景，服务器本身持有一个虚拟玩家，其余加入者均为非房主的普通成员
服务器的虚拟玩家接收并处理服务器逻辑，其余客户端全部处理普通Client逻辑即可

Transport文件夹：
每个文件都对应一个要在服务器和客户端之间来回传输的数据结构（class/struct都可以）
完成开发后按照EnsNetcode插件要求为其中每个数据结构在其文件内编写一个序列化器

Transport/CSPlayerInput.cs:
上行输入只有两条通道——CSMoveInput（WASD 的按下/抬起，边沿式、不可靠）与 CSActionInput（攻击/跳跃/滑铲/技能槽的按下，可靠）
客户端只上报原始按键（PlayerKey 位掩码，与 KeyCode 解耦），不解释含义；技能 id、瞄准点等全部由服务器算

GameController/Tool.cs:
通过static字段持有GameController中每一个Mono管理器的引用
提供工具函数

[E][T]GameController/NetworkManager.cs:
网络行为脚本，继承自EnsBehaviour
本项目唯一的进行服务器、客户端调用的位置
内置网络函数调用的例子
内置连接服务器进入房间的示例代码

GameController/InfoManager.cs:
持有对简易配置信息的引用，主要是ScriptaleObject，简易数据也可以直接在它的Inspector中配置

GameController/AssetsManager.cs:
简单的资产引用管理器，其中每个字段都持有对各类大体积资产的引用，如模型、特效等，非特殊内容全部通过它实现
其中的高复用内容通过GameController/Utils/AssetsObjectPool进行减少创建和销毁，AssetsObjectPool中持有资源类型枚举和整数id，根据这两者去确定资源类型。例如（类型为进攻方角色，id为1）等

GameController/Utils/VfxManager.cs:
全项目唯一的特效取用入口，两层结构：模板获取（GetXxxVfx(index) 返回特效预制体，按子弹/护盾/范围魔法/魔法阵/Buff五类区分）+ 播放（PlayXxxVFX(index, ...) 按下标取模板播放，或 Play(模板, ...) 对已取模板按定点/沿轨迹两种原语播放）。跟随类特效（护盾/Buff/光环）不用父物体挂载，统一用 FollowTrajectory(实体id, 偏移) 作为轨迹——服务器判定与客户端表现共用同一跟随关系；持续期用 Buff 剩余时长作 lifeTime，Buff 移除时销毁返回的实例。任何代码不得直接 Instantiate 特效或引用 AssetsManager 的特效列表，特效下标统一见《特效清单与分配表》

GameController/EnvironmentManager.cs:
昼夜只用「归一化周期时间 CycleTime [0,2)（0/2 = 午夜、1 = 正午）」+「dayDuration / nightDuration」两个时长描述
每秒推进量 = 1 / 当前区间时长（改时长只改速率，不跳变当前时刻）；当前为 90s / 90s
服务器权威推进，只在开战、时长或时间被改动、以及心跳时下发「时间 + 两时长」三值快照（SCDayNightInfo），客户端收到后自行推演
表现集中在 ApplyVisual：天空盒 _Exposure、太阳强度与旋转、雾气 Albedo、雾效 Fog Void FallOff（雾效开关由「迷雾」Buff 驱动）

Landscape文件夹：
地图基础信息与生成锚点
Landscape 为静态类：区块尺寸 chunkSize/chunkCount、地图边长 MapSize、中心点 MapCenter、InMap 判定，ChunkSearcher 依赖它划分区块。**可移动实体的区块索引每帧同步**（`BattleManagerCombat.TickMovement` 里对 `Entities` 无条件同步、对僵尸类别再补一次 `Zombies.UpdateObjectPosition`）—— 范围索敌（GetNearestEnemy / GetNearestInCamp）只查区块桶，不更新就会一直按出生区块找人。**`Beacons` / `Crystals` / `Towers` 三桶装的是静止实体，只在增删时定位，不逐帧同步**（新增会移动的实体类别时必须补上它所在桶的同步）。`ChunkSearcher` 内部用 `_chunkOf` 记录每个物体当前所在区块（位置会变，定位旧区块不能靠再读一次位置）
LandscapeSpawns 为挂在 Level 地形预制体内的组件，是**全项目唯一的地图点位来源**（守护点/水晶/防御塔/瘟疫树/僵尸出生点/双方出生-复活位置），InfoManager 不持有任何点位字段
点位共 7 组，两种存储形态：① **锚点（List&lt;Transform&gt;）**——守护点、防御塔、瘟疫树、进攻方与防守方的「出生/复活位置」（**出生与复活共用同一个列表**，开局出生与复活都从中随机取点、不去重）：在场景摆空物体后拖进列表，直接拖动即可调整，运行时读 Transform.position（列表允许留空位，读取时自动跳过）；② **坐标（List&lt;Vector3&gt;）**——水晶刷新点、僵尸出生点：由组件右键菜单按 **Terrain** 表面批量生成（水晶越靠 Terrain 中心越密、僵尸越靠边界越密；两者都保证周围 0.5m 无其它 collider 且不与已配置点位重叠）。列表整体为空时回退 Landscape.MapCenter
LandscapeSpawns **服务器与客户端场景各挂一份**，使用同一套锚点（锚点对象必须属于地形预制体，否则服务器那份引用会丢失）；服务器侧只提供点位、不含贴图与图形表现。Awake 时注册到 Tool.LandscapeSpawns，未注册时取用即报错

UI文件夹：
UI Tookit实现的ui系统的内容，全部放在其中
包含各类界面窗口，以及加载界面、消息提示、确认面板之类的通用界面
UITheme.cs 为全 UI 外观的唯一真值（颜色/字号/间距集中于此），各页面与 Unit 只引用它，不写字面量

Entity/EntityComponents文件夹：
EntityData为初始化入口，持有对技能控制器、buff控制器、实体动画等组件的引用，其它组件都依赖其中字段，因而需要在它初始化别的组件时传入它作为参数
EntityData要包含数据管理，例如受伤计算等，并且在单个实体触发特定事件（如被击杀等）时生成信息摘要传给BattleManager统一处理
Buff管理器（EntityEffectController）设计要点（对应策划案 11.3）：
1. 双轨属性，事件驱动：只有属性修改类 Buff（`IsAttribute` 白名单，**6 项**：力量 / 魔法 / 暴击率 / 暴击伤害 / 击飞抗性 / 可见距离，无特效）允许改属性。`EntityAttributeDelta.Field` 共 8 项，但**生命与武器槽位不在白名单内**（`EffectType.AttrWeaponSlot` 只保留枚举位，以免后续条目数值平移）。Add/Remove 时统一重算运行时属性（base + Σ修改量，当前生命夹到新上限，不自动回血）；其它 Buff 一律不得触碰属性
2. 查询式战斗管线：功能 Buff 不做每帧计算，由战斗代码在明确时机调用查询接口——受伤管线顺序 = 护盾吸收 → 减伤 → 受伤乘区（愈战愈勇；固定数值伤害跳过）→ 扣血 → 反伤；出伤乘区由攻击方在伤害计算时调用
3. OnUpdate 只做两件事：到期移除（Buff 记录结束时间戳 endTime，不每帧刷新剩余时间）、DoT/光环 tick（中毒/燃烧/死灵漫步均为固定数值、1s 间隔，吃护盾/减伤、不吃增减伤）
4. 强控（麻痹/冰冻/定身）施加：打断位移（MotionBase.Exit）、动画播放速度直接置 0（当前攻击随之被打断）；全部移除后恢复；期间霸体等级立即变为最低（GetEndureLevel 顺序：强控→None > 位移→Common > 强制霸体→Super > 动画状态）
5. 破霸体：攻击数据（AttackData.breakEndure）命中霸体等级 1 的目标时进入受击，且若目标在位移中则移除 MotionBase；命中判定入口 = EntityData.ProcessHit（**受击动作、打断位移、被击飞三者都在 enterHit 这一分支内**，见速度节⑥）
6. 沉默/强控期间无法释放技能（EntitySkillController.TryUseSkill 校验 CanCastSkill）
7. Buff 数据使用 struct；标记类 Buff（瘟疫标记/苍白之光/暗）无自身效果，供其它逻辑读层数（苍白转化由被动订阅 Buff 变化实现）
8. 全局参数（僵尸刷新等级、进攻方复活减速等）不是 Buff，实现于游戏逻辑控制器/Config（策划案第二十章）
9. 瘟疫祝福（`PlagueBless`）：唯一的"出伤 / 受伤乘区"Buff，与愈战愈勇同通道（`GetOutDamageMultiplier` +75% / `GetInDamageMultiplier` −25%），攻击方在伤害计算时读取。由 `PlagueTreeEntityData.OnKilled` 挂给"把树血量清零那一次攻击的攻击者"；被 Buff/DoT（固定数值伤害）打死的没有归属。移速类单独一档，见"速度"节⑨

[F]Entity/Anim:
它只是管理动画播放的，不处理任何复杂逻辑
**动画机（Animator）地位一律相同、不分主次**：实体上的每个动画机都承载同一套动作语义，任何动画机的行为脚本都按同一套规则参与"状态推送"与"速度声明"。不要按"主动画机 / 子动画机"去理解，也不存在"某个动画机更特殊"这回事——挂 AnimEvent、配 AttackType/threshold、声明速度，对所有动画机一视同仁。（`AnimEvent.Init` 的 `main` 形参只是让状态推送与速度声明由单个动画机统一发出、避免重复，不代表动画机有主次。）
通常开发只需要阅读其中的EntityAnim文件，其余文件不必管。该文件提供了控制动画的方法和动画会触发的事件（如特定时刻触发攻击）
动画同步以「状态的 fullPathHash」为唯一标识：AnimEvent 在 OnStateEnter 记录并下发，客户端 animator.Play(hash, 0, 归一化进度)，同 hash 不重播；客户端不需要也不传角色类型
AnimEvent 是纯推送制的唯一入口：EntityAnim.CurrentState 与同步用的 animId 都只由它在 OnStateEnter 更新，Do* 方法只写状态机参数、不直接改状态（任何动画机都按同一套规则挂 AnimEvent，见上）。因此**每个需要同步的状态都要挂 AnimEvent**（攻击状态用子类 AnimAttackEvent，逐状态在 Inspector 配 AttackType 与判定帧 threshold，双段攻击再加 useHit2/threshold2）。漏挂的状态既不会同步给客户端，也会让 CurrentState 停在旧值——霸体等级读的正是 CurrentState（EntityData.GetEndureLevel），会连带判错破霸体与受击

Entity/Attribute
角色属性信息，Info只是配置的内容，运行时只用EntityAttribute

[T]Entity/Identify
负责角色的各类标识，角色类型、阵营等信息

**阵营（EntityCamp，[Flags]）——攻击判定与索敌的唯一依据**
`EntityCamp` 是位标记枚举：`None=0 / Attack=1 / Defense=2 / Neutral=4 / Prop=8 / Zombie=16`。**实体自身只占一位**，而「敌对阵营」是可含多位的**掩码**，两者是同一个类型，传参时注意区分"我传的是单个阵营还是掩码"。

| 阵营 | 归属实体 |
| --- | --- |
| Attack | 进攻方角色 |
| Defense | 防守方角色 + 守护点 + 防御塔 |
| Neutral | 瘟疫树 |
| Prop | 可采集水晶（蘑菇是它的感染形态，同一实体） |
| Zombie | 普通僵尸 + 精英僵尸（夜刷与 PC106 召唤产出） |

敌对关系**只在 `EntityCampUtil.HostileOf(camp)` 一处计算**，禁止在别处再写阵营对立判断：
- 规则：敌人 = **除自身阵营外的全部阵营**；**若自身是中立或 Prop，再从结果中剔除中立与 Prop**；**僵尸另按单向特判**（见下）
- 得到的矩阵：攻 = {守, 僵尸, 中立, Prop}｜守 = {攻, 中立, Prop}｜僵尸 = {攻}｜中立 = {攻, 守}｜Prop = {攻, 守}
- `EntityCampUtil.IsHostile(camp, target)` = 目标是否落在敌对掩码内；`All` = 全部阵营位，`NonCombat` = 中立|Prop

**僵尸是唯一的「单向」阵营**：它只敌视进攻方，且**只有进攻方敌视它** —— 防守方 / 守护点 / 防御塔 / 中立 / Prop 都不与它敌对。"除自己外全是敌人"表达不了这种单向关系，故 `HostileOf` 对它单独特判（僵尸直接返回 `Attack`；非进攻方一律把 `Zombie` 从敌对掩码中剔除）。僵尸也**不为防守方提供共享视野**（视野分组用 `camp ==`，阵营不同即自动不成组）。生成时传 `EntityCamp.Zombie`：夜刷 `BattleManagerWorld.SpawnZombie`、PC106 召唤普通/精英僵尸。

**攻击判定统一走敌对掩码，禁止再写"非同阵营即命中"**（避免误伤友方）：
- 子弹命中 `BattleManagerCombat.TryHitBullet`、近战球判定 `SkillBase.StrikeSphere` → `EntityCampUtil.IsHostile`
- 范围索敌 `BattleManager.GetNearestEnemy`（内部取 `HostileOf`）/ `GetNearestInCamp` / `GetAllInCamp`、`SkillBase.AllInCamp` / `EnemiesIn` → **传掩码**，命中掩码内任一位即算
- 范围效果（泥沼 / 引爆瘟疫 / 迷雾 / 死灵漫步光环）与视野分组同规则

注意与「同阵营」判定的区别：**取友方**（`SkillBase.GetNearestAlly`、水晶掉武器的己方判定、视野"己方单位恒可见"）仍用 `camp ==` / 单阵营掩码，不适用敌对掩码。

⚠️ 连带影响（变化都落在中立与 Prop 侧；攻/守的判定与旧的"非同阵营"等价，行为不变）：
- 中立（瘟疫树）不再索敌中立与 Prop → 它的孢子弹不再误伤水晶
- 攻/守的**自动索敌掩码含中立与 Prop** → `AimPos` 的"最近敌人"可能在敌人之外锁到瘟疫树或水晶；「全场/周围敌方」类效果（泥沼 / 引爆瘟疫 / 迷雾 / 死灵漫步光环）同样会把它们计入目标
- 僵尸的敌对掩码 = {攻} → **不再误伤水晶与瘟疫树**（旧版"僵尸 = 防守方阵营"会把中立与 Prop 计入敌对）；防守方 / 守护点 / 防御塔 / 瘟疫树的敌对掩码都不含僵尸，因此也不会打自家僵尸

Entity/EntityPhysics.cs:
实体判定的唯一物理查询入口：按 InfoManager 配的层取掩码（`EntityMask` 实体层 / `GroundMask` 地面层），做球/胶囊查询并把命中解析成 EntityData（非实体的碰撞体直接丢弃）；结果先解析再返回，允许嵌套调用
近战用手部骨骼球，子弹用「上一帧位置 → 当前位置」胶囊覆盖整段路径防穿模
实体自带 Collider，不再有任何判定体积参数
【必须配置】InfoManager.entity_layer 要填专用层「Entity」（Layer 8）：层掩码按它左移生成，填 0 会与地形等 Default 层 collider 混在一起，查询缓冲被占满后会漏判
【必须配置】InfoManager.ground_layer 要填地面专用层：落地检测只查这一层（否则站在角色身上也会被判成在地面）。**地形与所有可站立的平台/物件都要挂到这一层**，漏挂会被判成永远悬空
客户端为表现而创建的基础几何体（如血条）必须销毁自带 Collider——宿主模式下服务器与客户端同进程，客户端物体同样会进物理查询

Bullet文件夹
子弹系统
玩法中所有攻击全部为各种类型的子弹，移动可跟随玩家/场景空间，可添加各类参数
注意：客户端不会实例化EntityData，处理移动应当通过id获取目标角色的transform
[E]BulletTrajectory 为弹道轨迹基类（abstract）：子弹系统（Bullet/BulletPlayer/BattleManager.ShootBullet）只依赖该基类，不关心具体轨迹。新增轨迹类型继承它并实现 Lerp(factor)（归一化进度 0~1 → 位置）即可，允许携带状态实现动态轨迹（如跟随目标）。现有实现：LineTrajectory（直线）、PointTrajectory（定点静止）、SkyFallTrajectory（升空超出视野后从目标位置天降）、BezierTrajectory（三次贝塞尔弧线/抛物线，GetProjection）、FollowTrajectory（跟随实体：位置恒等于目标当前位置+偏移，按实体 id 双端取位，护盾/Buff 等持续型特效与相关判定共用）。直线与定点不要用贝塞尔模拟，直接用对应实现。
轨迹需要在服务器与客户端按相同参数各自构建：服务器用 Bullet 做逻辑判定推进，客户端用 BulletPlayer 播放表现，两者结果应保持一致

Skills文件夹：
技能按「拥有者类型」分文件（不是按技能类型）：SkillPoolCrystal（摧毁水晶可获取的武器技能 0~49）/ SkillPoolDefense（防守方 6 角色专属 50~73，被动不占 id）/ SkillPoolNonPlayer（非玩家单位 100~179）/ SkillPoolUnarmed（空手攻击 180~182），统一在SkillManager.RegisterAll里注册
[E]外部调用只向SkillManager传入技能id即可。技能写法示例见SkillPoolCrystal中的SkillGaleSlash（疾风斩，id 0）。
技能双端执行模型：EntitySkillController.TryUseSkill是全部释放路径（技能槽/J键空手/AI/非玩家）的唯一收口——释放瞬间调skill.SkillLogic(entity)（技能内部生成上下文、并自行决定是否等动画攻击帧），拿到上下文后立即SendSkillCast同步给客户端；客户端经SkillManager.PlayVFX调同一技能的PlayVFX(context)，用同一个轨迹构建函数重建轨迹播放特效——伤害与特效必然一致。技能内禁止直接Instantiate特效或引用AssetsManager（资产下标映射集中在VfxManager）。
技能上下文（SkillContext）：标准弹道约定 = ints[0]施放者id、ints[1]轨迹形态（ProjectilePattern）、vectors成对存每发的(起点,终点)、发数=vectors.Count/2，见SkillBase.BuildShotContext；起点由服务器算好写进上下文（悬浮武器发射点依赖位置+朝向+槽位，客户端无法复原）。技能需要的额外信息（目标id等）自行追加。每个上下文只服务一次释放。
轨迹构建函数约定：技能写CreateTrajectory(context, index)，传入SkillContext、传出BulletTrajectory；SkillBase提供Line/Point/SkyFall/Arc四个常用实现。
多目标约定：ints[0]施放者id、ints[1..]依次为目标id（无弹道的技能用它传目标列表），SkillBase提供AddTargets/TargetCount/TargetId与PlayFollowAll（客户端foreach每个目标id播跟随特效）。服务器索敌可用EntityContainer的分桶（Entities/Beacons/Crystals/Towers/Zombies，可直接foreach）或SkillBase的AllInCamp/AllIn/EnemiesIn/NearestIn/GetNearestAlly。
命中附加效果：AttackData.addEffectEvent（命中时把目标的效果控制器交给技能挂Buff）与AttackData.onHit（命中时的额外逻辑）；子弹路径由BattleManagerCombat.TryHitBullet消费、近战球判定由SkillBase.StrikeSphere消费。SkillBase提供ApplyEffect（构造"命中挂Buff"回调）、GiveEffect（直接给实体挂Buff，用于护盾/增伤/净化等无需命中的场合）、PullTo（命中拉拽，走MotionToPoint）。**击退/击飞不是回调**：力度写在AttackData.knockbackPower上，由命中管线统一处理（见速度节⑥）。
持续型Buff表现：客户端按同步的SCEntityDisplayInfo.buffs增删特效，映射表见Config.buff_vfx（下标 = 特效清单编号 − 1），随视图销毁；技能无需为持续Buff写PlayVFX。
位移：统一走MotionBase（Entity/Motion/），用速度积分（EntityData.motionVelocity，BattleManagerCombat.TickMovement消费），禁止直接改position以免穿模；现有MotionDash（恒定朝前）、MotionToPoint（到点）。
**延时 / 多段 / 重复结算：统一用 `Timer.AddTimer`**（见 Utils 节），不要在技能里自己写计时字段——多段命中、延时爆炸、引导间隔、姿态持续期结束等都在 `SkillLogic`/`OnCast` 里注册即可，服务端与客户端（`PlayVFX` 内）都能调。Timer 没有取消句柄，注册的闭包请自行判空（实体可能已销毁）。
速度（**改速度前必读**）——**允许的来源只有五种，其它一律不允许**（尤其"用输入自己算速度"）：
（1）**动画声明**（见④）—— **玩家主动操控的速度只能来自这里**，动画没声明就没有速度；（2）**地面摩擦自然衰减**（见①）；（3）**技能 / 特殊效果位移**（MotionBase，见⑤）；（4）**重力**（见③）；（5）**击飞**（攻击命中的一次性速度覆盖，见⑥）。
移动速度的"快慢"（加速/减速/泥沼/死灵漫步）**完全通过调整动画播放速度实现**（见⑨）——不存在第二条速度通道。
① 水平速度的**唯一决策处** = EntityData.ResolveMoveVelocity（BattleManagerCombat.TickMovement每帧调用）：动画声明优先、**按声明值原样施加**（水平声明是世界空间水平速度；前后声明是角色本地前后的带符号速度）→ 离地则**保持**水平速度（无空气阻力、无空中控制）→ 未推进且在地面按Config.move_ground_friction（2 m/s²）朝0衰减。**没有"用输入自己算速度"的退化路径**：推进输入只提供方向与前后符号。
② 速度的**唯一写入处** = EntityData.SetMoveVelocity（只写X/Z，Y绝不写）；TickMovement把ResolveMoveVelocity的结果与motionVelocity相加后交给它。
③ 垂直速度：重力自动积分（SetupBody把useGravity置true；**Config.rb_drag必须为0**，否则空中会掉速、并在地面叠加出第二条衰减曲线）；起跳的向上初速度由**动画状态声明**（`AnimJumpStartEvent` → `EntityAnim.SetVelocityVertical(animData.jumpSpeed)` → `EntityData.SetVelocityVertical`），之后仍交回重力。
④ 声明速度的三个入口：EntityAnim.SetVelocityForward/Horizontal/Vertical 直接调用EntityData的同名接口（**不经委托**：动画可在高频处声明，走Action会白白多一次间接调用；客户端无EntityData，调用即忽略）——**具体数值完全由动画模块决定**；声明在"当前动画状态"内有效、换状态即失效（"动画没设置"即由此产生），垂直声明只生效一次。**前后 = 角色本地前后（局部空间，正朝前/负朝后，带符号）**，**水平 = 世界空间（x→世界X、y→世界Z，支持正负）**；垂直不乘系数。**前后与水平在声明时会乘当前动画播放速度（EntityAnim.PlaybackSpeed）**，垂直不乘。**速度声明的唯一发出点是动画机状态的行为脚本**（`AnimRunEvent` / `AnimJumpStartEvent` / `AnimJumpEndEvent` / `AnimRollEvent`；动画机地位一律相同，见 `[F]Entity/Anim`）——游戏逻辑**只触发动画与传参数、不自己产生速度**（唯一例外：击飞，见⑥）。
⑤ 位移效果（MotionBase，**不含击退**）：Enter/Update产出速度，**不吃输入也不吃任何速度系数**（冲刺不该被减速Buff缩水）；生命周期由EntityData.SetMotion/RemoveMotion管理，破霸体命中（ProcessHit）与强控施加（EntityEffectController）都会RemoveMotion。
⑥ 击飞（**命中瞬间的一次性速度覆盖，独立速度来源：不走动画声明、也不走MotionBase**）：`ProcessHit(attack, damage, isCrit, hitOrigin)` 里算 v = 攻击力度（AttackData.knockbackPower）− 目标被击飞抗性（EntityAttribute.knockbackResistance），两者**同量纲**、具体数值由技能与角色配置定；**v ≤ 0.01 直接不击飞**（v ≤ 0 一并落在此）。否则**水平与垂直的速度值都取 v**：水平方向 = hitOrigin 指向目标的水平方向（子弹传**上一帧位置**，防高速弹一帧穿过后方向反转；近战传判定球心），垂直向上（走SetVelocityVertical，之后交回重力，故击飞必然离地）。**击飞要判断霸体等级**：击飞写在 `enterHit` 分支内（enterHit = 霸体 None，或霸体 Common 且攻击带 breakEndure），即**霸体 2（Super）永不被击飞，霸体 1（Common）只有破霸体攻击才会被击飞**；同一分支还负责打断位移（RemoveMotion）与播放受击动作。击飞后空中保持水平速度，落地才按摩擦衰减（额外滑行 v²/(2×move_ground_friction) 米）。
⑦ 空中/落地判定：EntityData.UpdateGrounded（脚底小重叠球物理查询，**只查地面层 InfoManager.ground_layer**、剔除自身与trigger）写anim.InAir，**不用计时**判断落地。
⑧ 表现同步与客户端推演：服务器把刚体实际水平速度与yawSpeed随SCEntityDisplayInfo下发（BattleManager.FillDisplayVelocity），客户端在ClientEntityView.Update里按velocity/yawSpeed做包间推演（纵向不推演，位置仍以服务器包为准）。
⑨ 动画播放速度（EntityAnim.PlaybackSpeed = 播放倍率，强控暂停时为0）：既写 animator.speed，也在 SetVelocityForward/Horizontal 里缩放声明值——于是"走得快"与"动画播得快"天然同步，**加速/减速/泥沼/死灵漫步（Config.anim_move_speed_up/down/mire 与 death_stroll_speed_up）的效果就落在动画侧**（策划案11.3：这些效果的载体就是动画移动状态的播放速度倍率）。移速类清单收口在 EntityEffectController.IsMoveSpeedEffect，服务器聚合 = GetMoveAnimSpeedMultiplier、客户端孪生 = ComputeMoveAnimSpeedMultiplier，两处必须同步增改（新增移速 Buff 时别只改一边）。其它速度来源（MotionBase位移、重力、击飞）完全不受这几个Buff影响。
⑩ 子弹**没有**"速度"参数：位置由BulletTrajectory按"经过时长/生命"插值，快慢由轨迹总时长决定（调弹速就调时长或改轨迹实现），判定用"上一帧→当前帧"胶囊扫掠。
⑪ 相关配置：Config.move_turn_rate（转向角速度）、move_ground_friction、roll_cd、rb_drag（**必须0**）、rb_angular_drag（刚体旋转只锁 X/Z、**Y 轴不锁**，见 SetupBody）、anim_move_speed_up/down/mire（动画播放速度与声明速度倍率）。**移动速度、跳跃初速、击飞力度都不在 Config** —— 前两者是动画模块的模型数据（`EntityAnimData.RunSpeed` / `JumpSpeed`），击飞力度逐技能写在 `BuildAttack` 的 knockback 参数上。
⑫ 可移动判据：**挂了 `EntityAnim` 的单位才是可移动单位**（`EntityData.SetupBody` 与 `BattleManagerCombat.TickMovement` 用同一判据 `anim == null`）；水晶/守护点/防御塔/瘟疫树等非人形单位无动画，因此不挂刚体、不进移动循环。滑铲的**持续时长由动画模块自己决定**，外部不传时长。
索敌：SkillBase.AimPos为默认实现（视野内**本阵营的敌对阵营**中最近者，见Entity/Identify的「阵营」节；无目标则正前方Config.default_forward_aim_distance米），技能可覆写为自己的索敌逻辑。
轨迹实现需要实体位置时必须用BulletTrajectory.TryGetEntityPosition（只要位置）或TryGetEntityTransform（位置+朝向）（按实体id读取，双端通用），禁止直接读取EntityData组件（客户端不存在该组件）。
瞄准点不由外部传入：技能自己在SkillLogic里算（默认用AimPos，也可覆写）。

悬浮武器（技能三要素①，见策划案 13.1）：
位置 = 实体根物体的固定本地偏移（Config.weapon_float_offsets[8]，左右各 4），槽 i 用第 i 个；零挂点配置，任何实体通用（不依赖 EntityAnim）
远程技能发射点 = EntityData.GetWeaponFloatPos(本技能槽位)，无武器单位回退 BulletShootPos()（碰撞体 75% 高度）
SkillBase.WeaponInHand 标记近战类（武器临时到手，攻击动作结束归还漂浮）
客户端两套显示：ApplyHeldWeapon（按服务器下发的类别+下标，走到手部骨骼）+ ApplyFloatingWeapons（按技能槽技能 id 查表，常驻漂浮）

实体 AI（服务器侧）：
AI 的唯一落点是 `EntityData.TickAI()`（虚方法，默认空实现）——需要 AI 的实体自己覆写；`BattleManager.UpdateAI()` 每帧遍历全部实体调用它，**不按类别分支、不做全局节流**（决策频率由各实体在 TickAI 内按自身 id 错峰）
`TickAI` 只决定"去哪、放什么"：转向与推进仍由 `OnTickMove` 每帧负责，**AI 自身不产生任何速度**（速度五来源不变）。寻路共用 `EntityData` 上的 `MoveTo` / `StopMoving` / `ResolveNavDirection`（只借 NavMesh 算方向：`SamplePosition` + `CalculatePath`），**不用 NavMeshAgent**；目标没挪窝不重算路径，所以逐帧调用也安全。
**前置**：服务器场景必须烘焙 NavMesh —— 未烘焙时 `CalculatePath` 失败会**静默回退"直线朝目标走"**（不报错，但会穿墙/穿地形）。
另：AI 的移动意图靠 `OnTickMove` 每帧消费，而移动循环的判据是 `anim == null` 跳过 —— 所以**会移动的实体预制体必须挂 `EntityAnim`**，否则 AI 给出目的地也不会动。
`TickAI` 在 ManagedUpdate 中位于移动与子弹推进**之后**，因此能看到本帧最新位置；它释放的技能子弹要到下一帧 `TickBullets` 才结算

技能施法距离门闸（`SkillBase.CastRange`）：
`CastRange` = 有目标的攻击技能的施法距离（米，0 = 不限），由释放收口 `EntitySkillController.TryUseSkill` 统一校验：`CastRange` 内没有目标即拒绝释放
索敌阵营由 `HostileOf(施法者阵营)` 算出，**不得写死掩码**。近战技能自身不做距离判定（爪击是 `StrikeSphere(HandPos, melee_hit_radius)`），"够不够得着"全由这道门闸负责
自身增益类技能（如僵尸嘶吼）不设 CastRange —— 它们不需要目标，使用时机由 AI 决定

自动施法 AI（`AutoCastEntityData`，防御塔 / 瘟疫树继承）：
每隔 `AutoCastInterval` 秒从自身技能表里**随机取一个技能**尝试释放；**间隔是子类实现的抽象属性**（塔 / 树当前各 5s），便于逐类单独调整；首次释放按 `AIStaggerPhase` 错峰
本类**不移动、不产生速度、不自己判目标** —— "能不能打"完全交给技能的 `CastRange` 门闸（塔 20m / 树 8m），因此类里没有索敌代码
新增同类固定单位时继承本类并给出 `AutoCastInterval` 即可；会移动与追击的 AI 另走僵尸那条链（见下）

僵尸 AI（`ZombieEntityData`，普通与精英僵尸共用）：
行为骨架 `Wander`（无目标：出生点附近随机取点游荡，到点停 `Config.zombie_wander_pause` 秒）→ `Chase` → `Attack`
每次决策 tick 的**距离分档**：`dist > Config.zombie_max_chase_distance` 放弃并回游荡｜`dist > Config.zombie_roar_range` 追击 + 嘶吼｜`dist > Config.zombie_attack_range` 只追击不放技能｜`dist <= Config.zombie_attack_range` 停下、只放爪击（左右交替）
技能表槽位约定（顺序见 `Config.initial_skills`，普通与精英同构）：0 爪击右 / 1 爪击左 / 2 嘶吼
索敌半径为属性 `viewDistance`（未配则回退 `Config.zombie_acquire_range`）并夹到最大追击距离内，避免"刚索敌又要放弃"的空转；**已锁定目标仍有效就不换**——同优先级不换目标，避免来回横跳
攻击档停下后由 `OnTickMove` 的无目的地分支继续朝目标转向（只转不走），贴身时不会丢失朝向

AI 虚拟玩家（`PlayerEntityData.TickAI` → `PlayerAiController`，普通 C# 类，仅 `aiControlled` 时创建）：
`aiControlled` 由服务器在生成玩家实体后置位（`StartBattle` 与 `RespawnPlayer` 两处），真人玩家不受影响、输入仍来自网络
**目标选择只有一条规则：取「索敌范围内最近的敌对目标」**——敌方角色 / 守护点 / 防御塔 / 瘟疫树 / 水晶 / 僵尸一视同仁，纯比距离。敌对由 `HostileOf(自身阵营)` 现算，**因此攻守不需要行为分叉**：进攻方自然去拆守护点与防守方角色，防守方自然去拦进攻方
状态机三态：`Approach`（`MoveTo` 目标当前位置）⇄ `Engage`（到攻击距离内 `StopMoving` + `SetAiFacePoint` 只转向 + 循环放技能），横切 `Retreat`（血量比 < 30% 朝背离威胁方向退 25m）。**死亡即销毁实体、复活重建，所以没有 Dead 状态**
决策 0.15s（按 `AIStaggerPhase` 错峰）；**重选目标按 1s 节流**（要遍历区块桶），目标消失时立即重选
技能按槽位**从后往前**试，第一个成功即放（CD/库存/沉默/施法距离都在 `TryUseSkill` 内校验）；接敌距离取「优先使用的那件技能」的 `CastRange`，没配则用兜底值
`Retreat` 必须带**最长时限**（`MaxRetreatTime`）：游戏内**没有任何回血机制**（`EntityEffectController` 明确"不自动回血"），只看"血量回升"的话残血 AI 会一路逃到被打死